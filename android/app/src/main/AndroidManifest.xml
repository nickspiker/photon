<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.photon.messenger">

    <!-- Network access for messaging -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- MulticastLock (PhotonConnectionService): without it Android's radio firmware drops WiFi multicast, killing same-network LAN discovery. -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- Notifications (Android 13+ requires runtime permission) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Brief wakelock so the RX worker can run a headless protocol tick when a packet arrives
         while the screen is off (background CLUTCH/chat delivery; see docs/background-tick.md) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Pairing v2 proximity beacon (docs/pairing-v2.md): BLE advertise (new device) + scan (old device).
         SCAN/ADVERTISE are runtime permissions on Android 12+; neverForLocation keeps the location
         permission out of it. The legacy pair covers Android 11 and below. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <!-- Wi-Fi Direct bearer (docs/offgrid.md): off-grid friend discovery + dialog-free groups.
         NEARBY_WIFI_DEVICES is the 33+ runtime permission (neverForLocation keeps location out);
         ACCESS_FINE_LOCATION covers 32-and-below where the P2P APIs still demand it. -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="32" />

    <!-- Foreground service for persistent connection -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Voice calls (docs/calls.md): mic capture via VOICE_COMMUNICATION (the source that engages the
         vendor echo canceller). RECORD_AUDIO is a runtime prompt at first call; the microphone
         foreground-service type is required to capture while backgrounded (Android 14+). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- Sticky session broadcast — only apps signed with the same key may receive it -->
    <uses-permission android:name="android.permission.BROADCAST_STICKY" />
    <!-- Self-update (docs/updates.md): fire the system installer on a downloaded, hash-verified APK. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Unattended self-update (Android 12+): a PackageInstaller session with USER_ACTION_NOT_REQUIRED installs silently once photon is its own installer-of-record (one bootstrap confirm, then hands-free forever). -->
    <uses-permission android:name="android.permission.UPDATE_PACKAGES_WITHOUT_USER_ACTION" />
    <!-- NFC instant device add: reader mode (sponsor) + HCE dumb-tag (joiner). Neither feature is required — phones without NFC pair via words/BLE. -->
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />
    <uses-feature android:name="android.hardware.nfc.hce" android:required="false" />
    <uses-permission android:name="com.photon.SESSION_READ" />

    <!-- Signature-level permission gates who can receive the sticky session broadcast -->
    <permission
        android:name="com.photon.SESSION_READ"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".PhotonActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme"
            android:colorMode="wideColorGamut"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout"
            android:windowSoftInputMode="adjustNothing"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- FCM service for peer update notifications -->
        <service
            android:name=".PhotonMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Foreground service for persistent P2P connection; microphone type joins dataSync so call
             capture keeps running when the Activity backgrounds mid-call. -->
        <service
            android:name=".PhotonConnectionService"
            android:foregroundServiceType="dataSync|microphone"
            android:exported="false" />

        <!-- NFC instant device add: the joiner's HCE service serving the pairing secret under photon's private AID (res/xml/apduservice.xml). -->
        <service
            android:name=".PhotonHceService"
            android:exported="true"
            android:permission="android.permission.BIND_NFC_SERVICE">
            <intent-filter>
                <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE" />
            </intent-filter>
            <meta-data
                android:name="android.nfc.cardemulation.host_apdu_service"
                android:resource="@xml/apduservice" />
        </service>

        <!-- Exposes the staged self-update APK (app-private files dir) to the system installer. -->
        <!-- Self-update session result sink: pending-user-action bootstrap + success/failure logging. Not exported — only the OS PackageInstaller fires it via our own PendingIntent. -->
        <receiver
            android:name=".PhotonInstallReceiver"
            android:exported="false" />

        <!-- Post-update restart: an updated app is STOPPED and runs nothing until targeted; MY_PACKAGE_REPLACED is the system's you-were-updated hook (protected broadcast — exported is safe, only the OS can send it; exempt from the background-FGS-start restriction). Without it photon stayed dead after every update until an FCM doorbell happened to arrive. -->
        <receiver
            android:name=".PhotonUpdateReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
