---
name: project_identity_never_dies
description: "IDENTITY NEVER DIES (2026-07-17, SHIPPED): no terminal op — worker refuses zero-member folds, LastRites cut from code+docs; brands survive departure; two-signature retire (self-departure + member-signed device_release); retirement = obscure handle + puck (handle-change = future work)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 929b6108-6be3-4522-a24f-efa21c653e40
---

THE RULING (user, 2026-07-17, supersedes lifecycle D3's LastRites): **there is no way to end an identity — "how about we never."** Any terminal op, however ceremonial (red flood, handle re-entry lr-token, pending windows — all designed then CUT), is an annihilation surface for whoever holds a device + the handle (the daughter case). Deleting the op deletes the attack class. Wanting out = RETIREMENT: change the handle to something stupidly obscure and park the identity on a puck device. **Handle-change is the deferred future work that makes name-freeing a courtesy, not a death.**

Shipped same day (fgtw + fgtw-bootstrap deployed + photon, all pushed):
- Worker refuses any fold reaching zero members ("an identity must live somewhere"); purge_identity deleted; photon's Remove & shred REFUSES on the last member (toast: add another device first). LastRites flood/state/press-three all deleted.
- **Brands survive departure**: the device→owner claim is no longer dropped when a member departs. Sign-out ends the key's membership (sovereign self-record), never the hardware brand — a thief signing out of stolen hardware holds a branded husk.
- **Two-signature retire** (the add ceremony in reverse): device self-signs departure, then a SURVIVING member signs the `device_release` op (worker verifies signer folds as current member, released device must NOT be current, wrong-owner refused, already-free acks). Handle knowledge alone can never free hardware (eviction-backdoor rule).
- Photon fleet page: departed-but-branded rows render "retired — still yours" + per-row two-tap Release pill; released pubkeys accumulate in the fleet-synced `fleet.released` setting (concatenated 32B, binary at rest) so rows drop fleet-wide (chain can't distinguish released — it's permanent testimony).

Honest limits (stated, accepted): factory reset rolls the fingerprint-derived device key so the brand deters in-app re-homing, not reset-and-reattest (until PIPE); ocean-lost device = permanent ghost member (harmless — blind via re-key, tombstone icon is local-only per [[project_device_loaners]]).

Consequences: zero-fold purge, free-on-zero, and the "name returns to the pool" rule are all GONE (genesis-hash pin stays — custodian supersession + defensive rendering per [[project_total_loss_recovery]]). User also flagged: the Security page needs rework to match ([[project_lifecycle_flows]] build order shifts).
