---
name: project-render-storm-lag
description: ROOT-CAUSED 2026-08-15 - UI lag = vault mutex contention; per-tick avatar probe read blocked behind background persist writers; photon fix shipped, kete lock-narrowing queued
metadata:
  type: project
---

**OPEN:** field lag on 2026-08-15 during the unlock test — clicks up to 1s, occasional beachball, "everything laggy".
Evidence before the ephemeral logs swept: test-identity-Z = sustained back-to-back ~133ms renders labelled "other" (a redraw storm at 7.5fps, launch or settings screen); test-identity-E = scattered 17-66ms renders, mostly Conversation. ZERO check_clutch_* PERF lines — the decap offload holds; this is the render half.
Two questions: what requests redraw every frame in a non-Ready state (launch chromatic wave animates by design; settings pages should not), and why each frame costs 133ms there.
Probe fixed @e4bec90: render PERF now names the exact screen (per Settings page). Next: reproduce on the laggy device with a build carrying e4bec90, resubmit logs, pull TO FILE.
**Round 2 (fresh test-identity-Z log, saved to file):** the real eater is check_status_updates at 400-1794ms on the UI thread (test-identity-Z = MACBOOK fe46a74b, hence beachball); renders were downstream. The arm-loop pass profile (>200ms) stayed SILENT and no drain/arm line fired — the stall provably lives in the UNTIMED regions (pre-loop drain gaps or the post-loop deferred section). Ruled out: pings (channel send), maybe_generate_s (early-returns once S exists), clutch drains (clean). Region timers added @e283495 ('status pre-loop' / 'status deferred', >100ms); next lag log names the region, then fine-grained timers inside it. Stall context lines: RELAY delivered/DROPPED, GOSSIP merged 234 records, PHONEBOOK persisted 491KB, PT retransmits.

**ROOT CAUSE (round 3, region timers):** 52 'status pre-loop' hits median 492ms max 1658ms, zero deferred/drain/arm lines — by elimination the avatar sweep's per-tick has_cached_avatar_from_seed. The read takes kete's VAULT MUTEX, which persist workers hold thru whole encrypted table writes (491KB phonebook, message tables, chains) — every background write became a UI stall. The Aug-8 offload moved writes off-thread but UI-tick READS still queue on the same lock.
**FIXED photon-side @c6f65e8:** probe result cached per party id (one vault read per contact per session), cleared on avatar install/pin adoption; steady-state ticks are vault-free.
**QUEUED (the durable half):** narrow kete's critical section — manifestus put runs INSIDE the mutex (encrypt is already outside); readers should never wait thru a writer's file IO. Also audit remaining UI-thread read_addr call sites (fleet page fanout_pairs::load per frame is one).
**Unexplained residue:** one report of a click "sending it backwards" during the stalls — likely event queueing/stale hit-map under 1s frames; expect it gone with the stalls, re-open if it survives.

**SHIPPED (the durable half) — kete 931fd90 2026-08-16: THE LIBRARIAN.** One thread owns the manifestus engine, the mutex is deleted, ops ride an mpsc mailbox; reads are answered before each mutation so a read waits ≤ one in-flight write, never a persist burst. AEAD stays on caller threads; the public API and durable-on-return are unchanged (sync wrappers post-and-wait), so read-your-writes holds per thread by construction — a new 4-thread fat-write concurrency test pins it. Dependents green: rarangi 12/12 (2 integration failures = pre-existing VSF v8 fixture rot), photon 203 lib tests + both cfgs + the real-vault smoke bin.
**STILL QUEUED:** audit remaining UI-thread read_addr sites — with the librarian a stray read costs ≤ one write, but the fleet page's per-frame fanout_pairs::load is still wasteful.

**CLOSED (pending field confirmation) 2026-08-16:** stage 2 done @photon 8a9b886 — fleet page egged probe cached (invalidated at pair-mint), UI read-site audit found no other per-tick/per-frame reads. Full stack now: probe caches (UI touches vault zero times steady-state) + kete librarian (a stray read waits ≤ one write). Confirm with a test-identity-Z field log: expect ZERO "status pre-loop" lines; then this file closes for good.

**BONUS FINDS while closing (2026-08-16):** (1) photon's two "known" phonebook test failures were a LIVE vsf bug — empty 1D tensor (peer with no local_ip) poisoned the whole parse; the MacBook line had already fixed it (vsf 5479910) but the DESKTOP vsf checkout was stale (the sibling trap again) — fast-forwarded + regression tests added (ecde199); photon lib suite is now 205/0, first full green. The same bug was why the persisted phonebook went "unreadable, starting fresh" every boot. (2) Sibling sweep found tohu DIVERGED: the whole unattended-reboot capsule (8d66b19, recorded SHIPPED) was never pushed — stranded desktop-only like bridge.rs was; rebased onto the MacBook's macOS-capsule fix and pushed (d26a8d8).

**FIELD-CONFIRMED CLOSED 2026-08-16 (test-identity-Z android, v0.54.7):** ZERO "status pre-loop"/"status deferred" lines; check_status_updates p50=64ms p90=148ms max=350ms (was median 492 / max 1794 sustained). The probe caches alone did it — v0.54.7 predates the librarian AND the vsf fast-forward (both path deps, build was 17:58 Aug 15), which also means the one "phonebook unreadable, starting fresh" line in this log is expected: that build still carries the stale vsf. Next dev publish picks up librarian + vsf fix; expect the phonebook to load and the fresh-start line to vanish. Residual (different class, moderate): render >16ms lines on phone hardware (134× Ready, 83× Conversation, worst ~30ms) — frame-budget tuning, not stalls. FILE CLOSED.
