---
name: project-fleet-epoch-arc-closed
description: Epoch arc CLOSED @ fa3a9c0 2026-08-16 - hist_page+pong epoch re-seal, row-cadence mint; CAUGHT field_u64 exact-variant bug that had chain_sync + ALL ckpt frames parse-dead since B3
metadata:
  type: project
---

**SHIPPED @ fa3a9c0 2026-08-16** — the fleet-epoch B-arc residue closed, field verify pending:
- **hist_page re-seal**: fleet-route pages seal under `fleet_epoch_seal_key(epoch, b"hist_page")`, `ek` on the frame (OPTIONAL: absent = friend-route under the friendship history key). Receiver opens at k / k−1; behind → ckpt_req + drop (self-heals); sibling page without ek = flag-day drop. No spine = hold (the chain_sync rule).
- **pong re-seal**: sibling pong tails key off the epoch spine (`photon.pong.seal.sib.v1`); map reseeds on EVERY spine edge (mint / root hand-off / state adopt / vault load). Pre-spine = v0 static fallback.
- **row-cadence mint**: 128 fresh syncable rows → ckpt_mint_due in the fleet sweep (cheap len() sum vs base). Won fanout rotation also mints (PCS boundary). Before this the ONLY steady-state mint edge was fleet-join — the spine never advanced.

**THE PARSE BUG (4th victim of the silent-VSF-rejection class, after [[vsf-toc-section-name-trap]]):** `field_u64` matched `VsfType::u(n, false)` exactly, but auto-sized `u` DECODES as the smallest concrete width (u3/u4/…) — the match NEVER fired on a wire frame. chain_sync + ckpt_root + ckpt_req + ckpt_state all failed parse silently since B3 shipped: fleet chain replication and the device-to-device spine plane were DEAD in the field (the wiped mac "holding for ckpt_state" forever while the desktop served it = this). Fixed with `as_u64()`; round-trip tests over real encode+decode now cover every integer-carrying sibling frame. **RULE: never variant-match a parsed VSF integer — always `as_u64()`/`as_usize()`; and every new frame gets a build→parse round-trip test.**

**Field soak round 1 (2026-08-16 evening) found TWO wedges, both FIXED @ 2a2d267:** (1) spineless hold was permanent — dead k=1 custody (minter wiped, fleet key rotated past the seal) + B-arc holds parked ALL fleet traffic behind the missing spine; now ckpt_req per hold sweep + 3-strike SUPERSESSION (fresh seed at chain_k+1, worker CAS one winner, custody under current fleet key). (2) friend refused_devices had NO reversal — the lockout E2E left the desktop permanently deaf at Emma; pong locked-set gossip now retracts per-reporter (absent device = retraction, zero reporters = un-refused, empty set processed). **Round 2 soak (20:25-20:31): supersession WORKED** (benign double-mint k=2→k=3, custody under live key; fan-out epoch 44 wraps ALL 3 devices; roster steady at 10) — but the un-refuse NEVER FIRED: it gated on retracting a ledger pair, and locked_reports_seen is RAM-ONLY while refused_devices PERSISTS — every restart orphans refusals. FIXED @ fc47b18: orphaned refusal (zero standing reporters) yields to the first trusted pong whose locked set is clean. **Round 3: FRIEND-UNREFUSE FIRED** (Emma restored the desktop on the mac's testimony) — un-refuse arc CLOSED. New find: friend stuck relay-tier from the desktop while direct from the mac — resolve_stalled_addresses_from_seed gated on ip.is_none() and the RELAY SENTINEL (0.0.0.0) in the ip slot read as 'has an address', so a relay-only contact could NEVER upgrade itself (registry record was perfect; the desktop never asked). FIXED @ 41c053b (bogus ip = no address). pid-probe now dumps per-device registry records (pb_get + pb_devices view). Round 4: STILL relay-only — the resolve walk's 16-device budget RESTARTED AT CONTACT ZERO every pulse (same 3 resolved forever, head-of-list offline contacts starved the rest); FIXED @ d5bdd0f with a rotating cursor. Also: lane wedged at the in-flight window on PRE-FIX pendings (peer stored rows without ack_hash = un-re-ACKable forever); FIXED same commit — in-order lanes IMPLY-deliver everything older than an acked row (process_ack retains > acked; straggler ACK = no-op). v0.55.0 'Tera Luna' RELEASED 2026-08-16 (all 8 platforms, first deploy off Harbor) — carries the whole soak arc thru round 5. Round 5: BOTH round-4 theories incomplete. (a) 'resolved 3' unchanged with the cursor — the budget never exhausts; the record IS adopted yet the punch still omits the WAN; adoption now LOGGED per-tuple @ 5efe8c4 (round 6 answers it). (b) implied-ACK couldn't fire — the full window blocks the fresh send whose ACK would clear it (flushed 0/4); FIXED @ round-5 commit: clear_pending_up_to(peer lane tip) on the sync-record edge — the tip ('everything up to here in order') is the same testimony rearm already trusts. Lag report: render/pass times clean in logs; unreproduced. **Round 6 (2026-08-17) SOLVED the relay tier @ 770aeaf:** the adoption instrumentation showed the friend NEVER in the resolve set — the desktop hears her phone's multicast ACROSS subnets (bridged 192.168.0.x/1.x), the cross-subnet path validates and FLAPS, and the flapping ip counted as 'has an address'. Fix A: resolve's stalled predicate includes direct-dead (punch_unvalidated_cycles ≥ PUNCH_UNREACHABLE_THRESHOLD, hoisted to types::contact). Fix B: anchor-wedge detector drops the linkability exemption (exhausted at a live anchor = rotate; the pre-fix dedup-skip rows starved the cascade the exemption assumed). Tip-clear field-proven same round (Nick logged implied-delivered). Round 7: BOTH round-6 heals never armed — the flap's brief validations reset punch_unvalidated_cycles below threshold, and attempts were RUNTIME-ONLY so short sessions never exhausted the ladder. FIXED @ round-7 commit: attempts persist in the pending codec (round-trip tested; restart must not amnesty a dying lane), resolve gate = validated_path.is_none() (the one honest state). Round 8: resolve gate WORKS (her v6+LAN adopted and punched; neither answered — phone doze/v6 filtering — so relay is now honest). Attempts persisted to 7/8 ✓. NEW ROOT CAUSE for the starved tip pipeline: 127/129 of her Nick pongs hit the WRONG-RESPONDER salvage (fan-out answers from devices the ping didn't name) and both salvage arms dropped the sealed tail — ZERO sync records processed all session, starving tip-clear/anchor-heal/digest kicks. FIXED @ round-8 commit: salvage arms open the tail and pass sync records (replay-safe: old tip clears less); name/pin/locked stay matched-pong-only (nonce replay boundary). Round 9: salvage VERIFIED (5 records/pong) + Nick→Emma now DIRECT from the desktop and fast from both. Remaining stick = Emma→Nick: her lane's peer head is NONZERO-STUCK below the wedged rows (peer lane stalled where hers did; rows above are un-re-ACKable forwards) — tip-clear correctly no-ops, anchor detector correctly silent, exhaust→re-arm loops. FIXED @ round-9 commit: stuck-tip rotation — app-side ledger counts exhaust→re-arm ladders per advertised head; 2 ladders at one head = rotate (same evidence standard as the anchor, no new wire; head-hash TICKETS still the eventual precise form). **Round 10 (2026-08-18): SOAK COMPLETE.** Stuck-tip rotation FIRED as designed ('LANE: peer's head STUCK ... thru 2 ladders (4 pendings) — rotated', then flushed 1/1); messages fast BOTH directions. Last symptom (desktop shows friend via relay while the mac on her segment shows LAN) is the ROUTER's truth: her phone's direct pings reach the desktop from her .0.x address, the desktop's reflect-backs and punches (v6 + registry LAN) all die — desktop→phone unicast is filtered between the bridged segments. Tier display is honest; delivery is fast because her direct leg + relay cover both directions. Software done; fixing the ring = router config (allow inter-segment unicast) or same-segment.

Desync-repro audit (2026-08-16): the pinned cross-probe mechanisms ([[project-chain-advance-desync]]) are addressed in code — probe rows persist for re-ACK durability, are weave-ineligible (the fork vector), nonce fork-repair is the backstop. The LIVE field breakage was the parse-dead chain_sync all along. Two-device soak on ≥fa3a9c0 builds is the confirmation gate for clearing that memory.
