---
name: project_incall_learner
description: "in-call passive calibration learner (Nick's insight): echo g/delay + voice level learned from live calls; Stages 1-5 SHIPPED 2026-09-03, Stage 6 (gate off) BLOCKED on field proof"
metadata: 
  node_type: memory
  type: project
  originSessionId: 8c6221f3-48a5-4f77-b00e-1e3c7fa4c3b5
---

Nick's insight 2026-09-03: a live call contains both Wave measurements (far-talks-alone = echo coupling since WE rendered the far signal; near-talks-alone = natural voice level). Formal plan approved (gate off AFTER field proof; echo+voice together). Plan file: ~/.claude/plans/velvety-chasing-lemon.md.

**SHIPPED (photon c8b37a1→b8942c2, all green, 270 lib tests):**
- S1 c8b37a1: platform RENDER_ENV tap — (osc, env) per rendered frame, cursor-drained, ~10s, survives hygiene without cursor rewind.
- S2 dcd558f: call/learn.rs pure math + KATs. Load-bearing decisions (each KAT-locked): FLOOR-SUBTRACTION never mean-centering (keeps double-talk contamination one-sided → 25th-pct min-statistics valid; centered variant provably biases LOW); minimum-statistics floor (fall instant, rise 5%/tick); max-deviation stamp-regularizer slewing (per-frame slew drags lattice under bursts); alias defense = publish-time LAG CLUSTER on low-g pool half (PSR failed: speech plateaus give PSR≈1.05; delay-locked narrow scan failed: contaminated locks reject clean windows); per-gate reject counters (caught an i64::MIN cadence sentinel wrapping the estimator dead).
- S3 b8ea202: shadow wiring — engine feeds learner above the mute skip (muted = cleanest echo windows), teardown telemetry line "CALL: learner — g/delay/conf/windows/floor/talk/rejects".
- S4 0bc39f7: LEARNED mailbox (separate from ritual RESULT single-slot; toast-free drain), blend via learn::blend_g (asymmetric: duck-more full weight, duck-less ¼+solid-only), `<base>.n` sample-count keys, ritual writes n=0 (ritual outranks).
- S5 b8942c2: predictive duck — pred = g_norm × vol_lin × far_env(t−delay) via learner bins; PredGate hysteresis (enter pred×2, exit pred×3); fixed mic_gain replaces PID when voice profile exists; EngineParams.cal snapshot read UI-side (settings.rs cal_snapshot()); learner ARMS the duck mid-call at Usable; delay steps only between far bursts; live route tracking (mid-call swap finalizes learning, reverts reactive, re-evals headset bypass — fixed the cached route_ducks bug).

**FIRST FIELD WAVE 2026-09-03 (Nick↔Emma, 41s, both logs pulled): transport pristine (0 loss both ways), residual echo = STALE SEED MISMATCH both sides.**
Nick desktop: learner measured g 1.8436 @ 140ms but conf None at 9 windows (ONE short of Usable) → duck ran the stored seed g 0.8894 @ 40ms all call: delay 100ms early + g 2× low → mic ≈ 2×pred sits at the enter threshold → only 399 gated + 337 ducked of 1882 far-active frames; ~1150 far-active frames ran full mic thru a g≈1.84 coupling (Emma heard herself, tail of every phrase).
Emma phone: measured g 0.0180 @ 70ms, 10 windows but spread ≥1 → conf None; seed applied g 0.1320 @ 290ms → prediction looks 220ms too far back → burst ONSETS read far-silent → Full verdict → first ~200ms of Nick's echo passes (the "little echo-ey" Nick reported).
Convictions: (1) short waves (~40s) can't reach Usable, so the live learner never arms and NEVER corrects a bad seed — and persist is Solid-only, so the same wrong seeds return next wave (self-healing loop is open for short calls); (2) both ritual seeds' DELAYS diverge hard from live physics (40 vs 140, 290 vs 70) — delay is the robust early estimate (lag cluster), candidate: slew delay before g at sub-Usable; (3) Nick's mic stream had 11 hole-rejected windows + jitter peak 100/trims 209 — desktop capture stalls are the pool's biggest starver after the r-gate.

**FIELD WAVE 2 2026-09-03 (Nick↔Brittany, 10s): "a bit loud" convicted = the USAGE_MEDIA path.** Render rides USAGE_MEDIA (latency-first trade) → Android plays waves out the LOUDSPEAKER at MEDIA volume, never the receiver; Brittany's media vol was max (0.1dB) → near-unity coupling measured (g 0.9011 @ 60ms) and her hot mic (pre-gain 6647 vs 4000 target) sent Nick +15dB over the Emma wave (rx 7098 vs 1213). Her seed nearly matched live (0.964@90 vs 0.90@60) — duck did its best; loudness is path/volume, not duck. BUG: Kotlin pushRoute ranks AVAILABLE devices (earpiece>speaker) so the route label + calibration-profile KEY say "earpiece" while physics are loudspeaker — collides if a real receiver path lands. Desktop delay 140ms now confirmed twice (g 1.84 then 3.93) vs stale applied seed 40ms.

**OUTPUT PAD SHIPPED 2026-09-03 (Nick's call: "4 stops, disable the speaker section")**: live wave playback padded 4 stops down (engine OUTPUT_PAD_STOPS, `>>4` at rx enqueue) — the headset default; speaker toggle PARKED (handler/render/stamp/walk all commented, restore when a real speaker route lands). Ritual prompt + recording preview enqueue direct, unpadded; RENDER_ENV/REF taps sit downstream so learner g now measures against the PADDED emitted level — expect measured g to read ~4 stops lower than field waves 1-2 and rx(play) tallies likewise.

**Stage 6 BLOCKED on field proof**: a few real calls' teardown logs must show learner g/delay ≈ ritual values at Solid confidence (and a constant-double-talk call showing a starved pool). Then: remove the TX mute on uncalibrated routes (engine.rs "Uncalibrated route mid-call = mic-gated" skip), the call-pill gate (render.rs route_calibrated), Wave page copy → optional bootstrap ("calibrate now or let your first call learn it") + diagnostic. Wave stays: first-call-zero-echo bootstrap, controlled diagnostic (e.g. macbook's suspicious echo g=0.0000 — macOS mic DSP eats the loopback), headset verification.

Contracts: separation invariant (learner sees ONLY pre-gain pre-duck mic env + own binned far env; never duck state or far_level()); learned g stored at 0dB reference where a volume mirror exists (vol key 0.0), absent on desktop; audio.cal.* keys unchanged from the ritual.

**V-CHIRP CONNECT PROBE 2026-09-07 (supersedes Stage 6's plan + the ritual entirely)**: every call now OPENS with the probe — 250ms 200Hz→20kHz log sweep + its time-reversal overlaid (one template, both ends zero-crossing, no fades — device taper rounds it; raw sweep doubles as future loop freq-response probe), played unpadded on the live route while BOTH directions hold (no TX, RX decoded-not-rendered), then audio connects and the fit seeds the duck a beat later. Fit = matched filter per leg (lag mean = true delay skew-cancelled, lag DIFFERENCE = render-vs-capture clock skew, leg agreement = corruption gate, ~33dB gain vs "hello") + median per-bin envelope ratio for g (learner's unit, total leak not direct path). src/call/vchirp.rs (5 KATs); engine probe phase + Verdict mailbox (drained at engine start against stale-fit leak); persists thru post_learned solid-tier. REMOVED: Settings→Wave page (SettingsPage::Wave variant, render arm, driver slots, ~29 Msg variants ×3 langs), the whole doctrine (place-call gate, answer gate, ROUTE_CALIBRATED mic-mute mirror), ritual machinery in calibrate.rs (now: profile types + LEARNED mailbox + env/quietest_run only), cal-prompt.opusp + promptenc. Ringback probe KEPT (chirp verdict outranks it). Voice profiles now come ONLY from the learner. Overlap note: callee finishes probing ~RTT before caller → caller drops ≤~300ms of earliest callee audio (accepted, edge-correct).

**FIELD ROUND 2 (2026-09-08, Nick↔Brittany 38s, clear audio both ways)**: sibling fix + probe verified live. CONVICTED: (1) latency = JITTER RATCHET — target ended 11 (Nick) / 12=CAP (Brittany) = 110-120ms standing; growth +2/underrun vs decay 1/5s never recovers; FIXED: decay 5s→1.5s/step + tier-aware jitter min (target ≥ rx window frames, lifted immediately — floor-rung 4-frame bursts structurally underran a target of 1); (2) v-chirp fit rejected "legs disagreed" BOTH calls at 250ms → sweep lengthened to 1s (Nick: only the middle is audible; +6dB, 100 g-bins, reject gates now log lag/gain/psr detail); (3) Brittany's silent no-screen ring = call notification rode the MESSAGE channel (importance cached/user-degradable, IMPORTANCE_HIGH re-declare is a no-op) → dedicated "photon.calls" channel + Android 14 canUseFullScreenIntent() logged at post; (4) "ladder 16..-128 kbps" = vsf isize auto-size packed positive 128 into the i8 slot (unsigned-limit compare) — FIXED in vsf c0f3f1d + boundary KAT.

**LOCAL-SOURCE + NLMS 2026-09-08**: THE unifying conviction — jitter hard-ceiling pops from the queue FRONT; the 200-frame chirp dump got beheaded to ~9 frames (both field rejects: up-leg weak/late because its low-freq HEAD never played; down-leg's low-freq tail survived), same splice/trim warped ended-screen preview ("choppy and distorted") + ringback, and polluted call jitter tallies (trims≈chirp burst, peak 100=queue bound). FIX: audio::set_local_source — chirp/ringback/preview drain VERBATIM (no priming/growth/trims/splice); cleared when network audio takes over + at clear_queues. Preview now Play/Stop toggle (done-flag polled). NLMS SHIPPED (Nick: subtract first, duck second; no RLS): call/nlms.rs (2048 taps, PRE 512, μ .25, 3 KATs: seeded>30dB instant, blunt-seed converges>20dB, frozen never drifts); vchirp fit exports IR taps (xcorr vs SUM template/energy) → Verdict::Coupled{ir}; engine: RefRing off render_ref_since, sample-COUNT timelines + one-time osc anchors (wobble folds into PRE), adapt gate=far>DUCK_FAR_HALF && raw<far×ECHO_GATE_RATIO, Gate verdict demotes to soft duck while armed, route-swap disarms, teardown "CALL: nlms — erle XdB". NLMS arms ONLY on chirp Coupled (no profile = old duck world).

**FIELD ROUND 3 (2026-09-08 v0.83.4)**: local-source worked where reached (clean session: target 1, 0 trims, 0 splice) but TWO more beheadings found: (1) PLAYBACK_Q_MAX 100 < 200-frame 1s chirp — drop-oldest AT PUSH ate the front half (both rejects persisted) → bound 240 + chirp now FEEDS PACED (≤40 frames/pass from the engine loop, never dumped); (2) preview grid slotted spool packets by DRAIN-BURST stamps at 200 slots/s → collisions ate half the frames ("super garbled") → grid_from_records lattice-slots per channel (reanchor >50ms = real gap), fixes transcode too (old kept blobs garbled permanently). Play-during-engine-teardown was silently REFUSED ("press play, nothing") → preview_pending retry on tick. Scrub bar + tap-seek shipped (PlaybackHandle pos/total, play_spool_at decode-and-discard seek, raw hit id + call_seek_bar rect). LAN/WAN badge asymmetry: media rode pure LAN both ways; get_local_ip routes toward 1.1.1.1 = CELL on dual-radio → our_v4 None → foreign-LAN verdict → WAN badge; fixed with get_local_ip_toward(peer). OPEN: Nick's phone reports vol -43.4dB while call audio sounds normal — STREAM_MUSIC index→dB mapping suspect (poisons chirp g_norm + emitted probe level reads).

**FIELD ROUND 4 (2026-09-08 v0.83.53, PRE volume-fix c19da72)**: (1) SCRATCHY ROOT-CAUSED = 2x TX from Brittany's phone: her tx 15808 frames/40s = 2x realtime, Nick's phone rx 15760 + trims 7699 (threw away half at playout). Nick's OWN tx 7716 = correct 1x, so device-specific to HER phone, NEW with 5ms flag day. Hypothesis: her HAL gives 24kHz for a 48kHz VOICE_RECOGNITION ask → each 240-sample read = 10ms pushed as 5ms. Added Kotlin capture log hwRate/askRate + engine "CALL: cadence — tx/rx fps" to pin it. (2) NLMS armed at g 6.2465 (the −43dB vol division) with −17.8dB ERLE = INJECTING echo (misaligned garbage seed from a barely-passed quiet chirp) → SELF-DISARM shipped (is_net_harmful: post>pre after 100 adapted frames → drop, duck carries). c19da72's low-vol chirp boost should prevent the garbage seed; the disarm is the belt-and-suspenders. (3) DESKTOP + MAC RANG FOREVER (3.5min): phone answered 03:25:02, pushed answer lane to "3 siblings (0 unreachable)", but desktop log shows ZERO receipt all call — sibling-answer ring-stop delivery GAP (not the merge→on_call_signal path, which is wired; the row never arrived). FIX DIRECTION: caller broadcasts a nonce-tagged Taken to the callee identity's device fan-out on going Active (rides the proven offer fan-out path, not the callee's internal replication) — winner ignores (its nonce), losers stop. NOT built. (4) Brittany notification-answer didn't connect + phone shows only lock screen despite fullScreenIntent=granted — NOT diagnosed.
**FIELD ROUND 5 (2026-09-08 waves)**: Emma↔Nick 93.5s LAN call clean (9056/9040 pkts, 1 window lost, 196 fps both ways, ladder 128k, chirp g 0.27 @ 260ms, NLMS self-disarmed −2.7dB → duck carried). The 2x-TX mechanism CONVICTED on Nick's phone (17:22 call: tx 390 fps, "capture up" logged TWICE with one "stopped" between, 5ms apart): the answer edge stops the ringback probe's audio and starts the engine's within one AudioRecord read, the flag flipped false→true before the old capture thread saw it → two capture threads fed the encoder. Fix: per-thread `audioGen` generation token in PhotonConnectionService (capture + render loops exit when not current). Brittany's 2x is very likely the same race, not a 24kHz HAL. Jitter on Emma's end: underruns 22, trims 37, splice −14827 — watch next round.
**FIELD ROUND 6 (2026-09-09 01:55, Brittany→Nick 54s, v0.86.2 vs her v0.83.5)**: transport clean both ways (5.1k pkts, 194 fps, 128 kbps; Nick rx lost 46 windows = the two UI hangs below). Tuning findings: (1) Brittany's chirp fit landed AT the delay grid bound (1500 ms; capture anchor ~1.4 s after render start) and was persisted as a solid profile → her predictive duck muted her 1.5 s after each of Nick's phrases — FIX: a fit at the bound is rejected (no seed, nothing persisted) and the chirp line now prints anchor-offset + acoustic lag separately. (2) Nick's seed g 7.67 @130ms (unit-volume g at low media volume ×2.83 boost) vs learner g 1.61 @0ms over 2 windows — never Usable (USABLE_N=10 windows; a 54s call gives 2-5) so a bad seed is never corrected: the standing learner-arming question. (3) Two 1.8s/1.6s UI hangs = recompute_ceremony_owners saving 13 contacts synchronously on a presence verdict — FIXED (derived state, never persisted; heavy-weave arming saves off-thread).

2026-09-12 evening, the echo profile from first principles (Nick's question): the chirp measures the local hop well (0-20 ms, per-band coupling); the NLMS (2048 taps, mu 0.5, 3-scale whitener) disarms as net harmful on every call because it adapts on the GATED mic (its competitor's output) for ~10% of frames; the gate is broadband level-only and cannot tell double-talk from echo at coupling near 1. Two loops: the round-trip loop (latency sets decay; loop gain = product of couplings — keep single-digit latency) and the local hop (the canceller's job). Shipped today: bounded hard-mute (GATE_RUN_MAX 0.5 s) + reactive fallback on disarm. PLAN, in order: (1) train the canceller on the clean take the ordering fix records; (2) subband canceller, ~32 bands over 10 ms, per-band step size, chirp's per-band coupling as seed; (3) per-band gate. Test waves to request: double-talk, normal hold at real volume, speakerphone both ends. Decisions for Nick: cap the earpiece rocker for waves or warn above coupling ~0.3; is speakerphone a supported mode.
