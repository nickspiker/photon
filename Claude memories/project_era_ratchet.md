---
name: project_era_ratchet
description: ERA RATCHET (re-key without wiping): plan at ~/.claude/plans/logical-brewing-creek.md, stage 1 SHIPPED 2026-09-08 (era model on the chains blob; re-key never blanks the friendship; completion supersedes, old era RETIRED read-only); stages 2-6 pending (wire era tag + pong era row, light ratchet ML-KEM-1024+X25519+HQC-256, woven full CLUTCH on shrink, consent-gated fresh, timers→edges)
metadata:
  type: project
---

Nick's mandate 2026-09-08: the re-clutch on an existing conversation kept wiping the live chain (Emma: "cannot send — no friendship chain", express dead, compose bar gone). Design: the era is a first-class generation; next era = KDF(old lane_root ‖ old history_key ‖ fresh secret ‖ transcript) — continuity/splice-resistance from the old root, confidentiality ONLY from the fresh secret (the reservoir is a pure function of lane_root + public history, so the old root IS the reservoir's secret — Nick's reconstruction point, confirmed in chain.rs).
Decisions: shrink (Revoke/Release) → auto re-key every friendship with a FULL CLUTCH woven with the old roots; from-scratch CLUTCH on an existing conversation → consent (knock) + banner, old chain live until completion; light ratchet KEM = ML-KEM-1024 + X25519 + HQC-256 (~9KB/16KB rows); standing cadence every 256 rows (edge, not timer).
Stage 1 shipped (commits 73464a3 + next): FriendshipChains gained era_index, era_lineage, per-lane lane_eras, retired: Option<RetiredEra>, pending: Option<PendingEra>, rows_since_ratchet; primitives install_pending / cut_over_to_pending / supersede_with / drop_retired_era / ensure_lane_era / era_slot_for_{label,tag}; era_superseded_by = index within lineage, genesis across; storage additive v8. The two offer arms + the removal block in status.rs and initiate_friend_rekey no longer touch friendship_id or delete chains; ceremony completion calls supersede_with (old era retired, RETIRED_ERA_GRACE_ROWS=64 current-era frames then dropped); express tries the retired key.
Deviations recorded in the plan file: heavy path supersedes at completion (pending+ACK gate is for the light ratchet only); cross-lineage sibling adopt keeps newer-genesis-wins.
Stage 2 SHIPPED 2026-09-08: chat-frame `era` tag (name-keyed, legacy-tolerant; RX routes label-first then tag; unknown tag drops pre-decrypt, never fork evidence), pong `era` row (SyncRecord era_index/era_tag), StaleEraObserved change-edge + retire edge (peer on our era + ≥1 current-era frame), era_pull (chain_pull + `era` field, serve iff newer), src/ui/photon_app/era.rs (friendship_repair pure/table-tested + repair_dispatch: acts on PullFleet, LOGS LightRatchet/HeavyWeave/ConsentFresh), Contact.peer_era_capable (`era_cap`). NEXT: stage 3 light ratchet (src/crypto/era.rs), stage 4 woven CLUTCH + shrink hooks (after fleet-key rotation; chain_sync opens k−1!), stage 5 consent/banner/pinned_genesis, stage 6 timers→edges + soak. Fix-in-passing noted: ClutchKemSharedSecrets::zeroize misses frodo1344/sntrup/p521.
FIELD 2026-09-08 (Emma/Nick "waves" logs, after stage 2): chats + a 93s LAN call rode a Pending-ceremony friendship the whole time — stage 1 holds. Two defects found and fixed the same day: (1) lanes were stamped with the era INDEX, and a fresh ceremony over an existing friendship is index 0 again, so `drop_retired_era` took the NEW era's lanes down with the old — stamps are now the root's TAG (loader maps index-stamped v8 blobs); (2) a round completed on every re-sent offer/KEM (slots stay full) and a second run after our own re-encap minted DIFFERENT eggs ("3dbe5401 → 3dbe5401", sibling PROOF MISMATCH storm, 436 re-serves) — `Contact::ceremony_completion_hold` makes a round complete once, `supersede_with` is a no-op on the same root.
OPEN (= stage 3's computed era_owner, now the priority): Emma's contact for Nick sits Pending; every app start she re-offers and ALL THREE Nick devices (desktop 90e571bf, phone cacbc223, MacBook fe46a74b) answer with their own instance → ceremony_id mismatch → never completes (§4.2 with an UNCLAIMED owner: ceremony_parked_by returns false when ceremony_owner is None). The friendship works only because the old era keeps carrying traffic.
Stage 3 SHIPPED 2026-09-08 (same evening): src/crypto/era.rs hybrid KEM light ratchet (ML-KEM-1024 + X25519 + HQC-256; next era = KDF(old root ‖ old hk ‖ fresh ‖ transcript)), EraSignal Init/Resp/Nudge hidden rows with KEM material in typed package fields ekn/ekx/ekh, responder cutover on the Resp's ACK or the peer's first pending-tagged frame, initiator on the Resp; COMPUTED ceremony owner (era_owner = lowest unlocked not-probed-offline fold member, recomputed on presence/fold/locked/roster edges; the offer arm parks non-owners at the wire — the fix for Emma's three-instance stall); cadence = every 256 peer rows. Decap runs inline (few ms) — off-thread is a later polish. NEXT: stage 4 woven CLUTCH + shrink hooks, stage 5 consent/banner/pinned_genesis, stage 6 timers→edges + soak. Field tells: "ERA: ceremony owner → …", "Init sent", "Resp sent", "cut over … on the Resp / on the Resp's ACK", and Emma's contact leaving Pending.
Stage 4 SHIPPED 2026-09-08 (evening): woven full CLUTCH — `from_clutch_woven` folds the fresh birth pair thru derive_era_keys with the prior era (index+1, same lineage); the offer carries an era-prior claim (prior_tag/prior_idx, folded into the provenance) that a peer echoes iff it holds the era (else not answered: retired = behind, unknown = StaleEraObserved); completion weaves only when both claims name the era we hold; SHRINK EDGES (fold shrink in reconcile_fleet_siblings, locked-set growth in apply_locked_set) arm `era_weave_due = epoch+1` per friendship (persisted), the keygen pickup runs it once the cached fleet epoch has moved (chain_sync opens k−1!); HeavyWeave verdict arms one friendship now. Zeroize gap (frodo1344/sntrup/p521) closed. NEXT: stage 5 consent/banner/pinned_genesis, stage 6 timers→edges + soak. Field tells: "ERA: heavy weave armed for N friendship(s) at epoch ≥ E", "heavy weave armed for X from era#N", "offers a weave from era#N — accepting the weave", "ERA: weaving era#N into this ceremony", supersede line showing index N+1.
