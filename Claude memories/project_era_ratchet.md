---
name: project_era_ratchet
description: ERA RATCHET (re-key without wiping): plan at ~/.claude/plans/logical-brewing-creek.md, stage 1 SHIPPED 2026-09-08 (era model on the chains blob; re-key never blanks the friendship; completion supersedes, old era RETIRED read-only); stages 2-6 pending (wire era tag + pong era row, light ratchet ML-KEM-1024+X25519+HQC-256, woven full CLUTCH on shrink, consent-gated fresh, timers→edges)
metadata:
  type: project
---

Nick's mandate 2026-09-08: the re-clutch on an existing conversation kept wiping the live chain (Emma: "cannot send — no friendship chain", express dead, compose bar gone). Design: the era is a first-class generation; next era = KDF(old lane_root ‖ old history_key ‖ fresh secret ‖ transcript) — continuity/splice-resistance from the old root, confidentiality ONLY from the fresh secret (the reservoir is a pure function of lane_root + public history, so the old root IS the reservoir's secret — Nick's reconstruction point, confirmed in chain.rs).
Decisions: shrink (Revoke/Release) → auto re-key every friendship with a FULL CLUTCH woven with the old roots; from-scratch CLUTCH on an existing conversation → consent (knock) + banner, old chain live until completion; light ratchet KEM = ML-KEM-1024 + X25519 + HQC-256 (~9KB/16KB rows); standing cadence every 256 rows (edge, not timer).
Stage 1 shipped (commits 73464a3 + next): FriendshipChains gained era_index, era_lineage, per-lane lane_eras, retired: Option<RetiredEra>, pending: Option<PendingEra>, rows_since_ratchet; primitives install_pending / cut_over_to_pending / supersede_with / drop_retired_era / ensure_lane_era / era_slot_for_{label,tag}; era_superseded_by = index within lineage, genesis across; storage additive v8. The two offer arms + the removal block in status.rs and initiate_friend_rekey no longer touch friendship_id or delete chains; ceremony completion calls supersede_with (old era retired, RETIRED_ERA_GRACE_ROWS=64 current-era frames then dropped); express tries the retired key.
Deviations recorded in the plan file: heavy path supersedes at completion (pending+ACK gate is for the light ratchet only); cross-lineage sibling adopt keeps newer-genesis-wins.
Stage 2 SHIPPED 2026-09-08: chat-frame `era` tag (name-keyed, legacy-tolerant; RX routes label-first then tag; unknown tag drops pre-decrypt, never fork evidence), pong `era` row (SyncRecord era_index/era_tag), StaleEraObserved change-edge + retire edge (peer on our era + ≥1 current-era frame), era_pull (chain_pull + `era` field, serve iff newer), src/ui/photon_app/era.rs (friendship_repair pure/table-tested + repair_dispatch: acts on PullFleet, LOGS LightRatchet/HeavyWeave/ConsentFresh), Contact.peer_era_capable (`era_cap`). NEXT: stage 3 light ratchet (src/crypto/era.rs), stage 4 woven CLUTCH + shrink hooks (after fleet-key rotation; chain_sync opens k−1!), stage 5 consent/banner/pinned_genesis, stage 6 timers→edges + soak. Fix-in-passing noted: ClutchKemSharedSecrets::zeroize misses frodo1344/sntrup/p521.
