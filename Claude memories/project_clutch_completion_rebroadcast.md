---
name: project_clutch_completion_rebroadcast
description: CLUTCH completes crypto-correct but rebroadcasts its proof forever; fixed with a hidden chain-weave probe that seals on first bidirectional ACK
metadata: 
  node_type: memory
  type: project
  originSessionId: 71fe9349-599c-4d2c-9970-3a988ee9a08f
---

CLUTCH reaches matching proofs (crypto done, both sides save Complete state) but the completion handshake never settles: the initiator re-sends its proof every ~30s indefinitely, and the responder — after 5 `ClutchComplete` resends (~15s) — clears its ceremony state and can only answer "Duplicate proof but ceremony cleared — cannot re-send". Root cause: the ceremony state machine is DECOUPLED from the chat data plane — real messages flow + advance the chat hash chain (proven in the Nick↔James log: "fish"/"with" both ways with ACKs) while the ceremony keeps re-announcing, because nothing wires data activity to ceremony completion. Distinct from [[project_clutch_offer_deadlock]] (that's the OFFER stage; this is the COMPLETE/proof stage).

Fix (implemented 2026-07-05, compiles clean, NOT yet device-verified): a hidden **chain-weave probe** — on `ClutchState::Complete`, each device auto-sends one `CHAIN_PROBE_MARKER` chat message that rides the ratchet unchanged but suppresses its UI bubble. Seal (`Contact.chain_woven`) fires once BOTH directions are proven (`their_probe_seen` + `chain_advanced_by_ack`), which zeroes `clutch_proof_resends_left` and flips the top status from "testing · weaving the chain" → "secured". Any real message also seals (belt-and-suspenders). The proof re-arm at photon_app.rs ~8338 is now guarded `if !contact.chain_woven`. New Contact fields are runtime-only (not persisted). One-probe-each-way validates hop 1 only — the msg-2 ratchet desync in [[project_chain_advance_desync]] would slip past it; bump to 2 probes each way if catching that becomes the goal.

**2026-09-10 FIELD EVIDENCE (Esme/Nick logs), UNFIXED:** every device restart re-runs friend/sibling ceremonies ("zombie round expired — AwaitingProof with nothing left to send… fresh ceremony", 600 s stale) and each completion mints a FRESH index-0 era (`prior` = None unless both era-prior claims match): Nick's phone logged "ceremony superseded era X → Y (index 0)" ten times in an hour (01:14, 01:20, 01:26, 01:32, 01:49, 01:54, 01:57, 02:00, 02:04, 02:10). Proof exchange loops: the phone "Ignoring duplicate proof — chain already woven" while Esme sits AwaitingProof and re-sends KEM responses the phone discards ("arrived before keygen — encrypted to old keys"); era_pull asks for "an era newer than #0" and every era is #0, so the fleet can never answer it. The call plane now tolerates one era of skew (6402ef0), but the churn itself is the disease: a re-completed round for an already-woven friendship should either re-derive the SAME era or weave from the held era, never a fresh lineage. Also: my dev.sh reload of the desktop is what kicked the 02:09 ceremony — desktop restarts are field events.

**2026-09-10 REPAIR SHIPPED d0ace9c (messages dead both ways, Emma/Nick):** era_pull now carries the asker's era TAG; a sibling holding that same era answers miss (the responder's `era_index >= held` served the asker its own era back forever). A sibling's chain-sync push while a pull is open either moves our era (pull answered) or counts as that sibling's miss (`era_pull_miss_from`). FleetAllMissed classifies against the peer's last-advertised era (`era_peer_seen`); Foreign → ConsentFresh → `arm_heavy_weave_for` (completes as a fresh channel; one round at a time). Consent gate (stage 5) still unbuilt. Caveat: a fleet device on an OLD build that wins the owner election (lowest pubkey among live/unprobed, e.g. 1be949c1) holds forever — every device must carry this build. The churn generator (restart → zombie round → fresh index-0 era, proof mismatches between siblings) is still open.

**2026-09-10 ROOT CAUSE CONVICTED + FIXED f001b85 (desktop log /tmp/photon-nick/photon.log.vsf, plaintext, read with `photonlog <path>`):** a friend's re-key offer at a Complete device opens a round (Pending, fresh keypairs, KEM sent); a sibling's chain-sync push of the SAME era lands mid-round → drain_chain_syncs flipped the contact Complete ("adopted chain — can now transmit directly"); the decap then hit `ceremony_completion_hold` = "clutch_state != Pending → already produced its eggs" → round dead. The peer completes alone and moves era; our whole fleet stays behind → era skew → messages/waves dead. FIX: same-era replication leaves an open round Pending (era_moved discards it instead); the guard holds on a HELD eggs proof or Complete-without-keypairs. The restart/zombie churn is still real but no longer strands fleets.
