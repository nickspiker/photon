---
name: project_bridge
description: "BRIDGE = passless remote shell between fleet siblings riding the regular chain; SHIPPED 2026-08-22; PIGEONS (drop a file on the bridge → lands in the host shell cwd via the zero-bitmap spool over PT) BUILT 2026-09-17, field test PENDING"
metadata: 
  node_type: memory
  type: project
  originSessionId: f7df2de2-8ae4-45a0-a572-2865a6e4ac5f
---

BRIDGE = passless remote shell between fleet siblings (rustdesk/SSH replacement). The 2026-07 term-frame model (BridgeHost/forkpty/term_* frames) was EXCISED — it fire-and-forgot and never delivered. The shipped model (2026-08-21/22) is chat-as-shell: commands are ordinary messages in the per-sibling conversation riding the regular chain (retransmit/ACK/re-serve/dedup), bare commands (no `$`), typed RefKind::BridgeOut/BridgeReset (never content sentinels), off-thread persistent bash per sibling (cd/env persist, 30s anti-wedge timeout), first-receipt-only execution (is_new_row gate = no replay), works over relay.

Ephemeral terminal SHIPPED 2026-08-22 (270fc46 + 4a316ca): sibling lanes are ANCHOR-ONLY (weave zero strands, need none on receive), so bridge rows are never persisted and open wipes the screen + rotates away stale in-flight frames; host mirrors on BridgeReset receipt.

TWO HARD-WON INVARIANTS (both field-burned 2026-08-22):
1. The braid weaves against prior rows — ephemeral rows + woven strands = "braid strand miss" = frames held forever (no display, no ACK). Ephemeral REQUIRES anchor-only.
2. NEVER bare-clear pending_messages — each frame links the prev hash; clearing mid-chain leaves a hole the peer gap-buffers behind FOREVER ("expected prev X — buffering (ahead of us)", zero ACKs ever after). The sanctioned abandon = rotate_our_lane ([[lane-rotation-wedge-heal]]): retire the lane wholesale, peer links the fresh lane from its ANCHOR.

DELTA STREAMING (Nick's redesign 2026-09-03, after the silent v82 deploy where the operator saw NOTHING thru 52s of cargo output): frames carry only what's NEW ("just send what's missing") — the snapshot re-broadcast + its duplication is gone. Host spools per-command unsent output (bridge_partials buffer, 64KB bound with front-trim COUNTED into the frame's elision marker), broadcasts at most 1Hz (the one granted timer), ONE delta in flight gated on its OWN ACK edge (bridge_partial_inflight eagle_time watched in pending_messages — whole-lane pending>=2 was the starver: fleet-sync chatter froze the feed). A parked/refused send puts the spool BACK — flow control never drops transcript bytes. "Finished" is a FIELD not a message: the exit rides whatever delta is last (wire bdelta u0 flag + existing bexit; old parsers discard unknown fields → pre-v82 clients degrade to replace-semantics, showing just the latest chunk). Client appends delta frames to the one command row (seq-guarded against re-serve double-append, 64KB front-scrollback trim); Stop pill stays exit-gated. Quiet window = zero frames (ten silent minutes = nothing on the wire).

HEADLESS LIFELINE SHIPPED 2026-09-07 (docs/headless-lifeline.md, from the 3-day black-greeter incident: Xorg SIGABRT on rustdesk-XTEST-cursor × HDMI-hotplug race in amdgpu DDX killed the session + bridge; SWcursor vaccine applied): `photon --lifeline` = display-free core (core_init split out of FluorApp::init, compiler-proven ctx-free) + condvar pump around advance_protocol; capsule auto-attest; control verb "yield" hands the instance lock to a full-UI launch (full-UI resident ignores yield, just surfaces); photon-lifeline.service user-unit watcher enrolled on leviathan via scripts/lifeline-unit.sh. Tier 2 SHIPPED same day, NO winit patch needed (winit 0.30 x11rb already exits the loop, not the process): fluor run_app_recoverable returns the living app; photon main drops it into run_lifeline on Err (same session, zero gap; core_init skipped via handle_query guard; control CURRENT_PROXY swappable so yield reaches the embedded pump; tray double-spawn guarded). UI enrolment = "bulletproof bridge" checkbox, Security page (platform::lifeline: Linux systemd user unit / macOS KeepAlive LaunchAgent). X-death E2E verification owed (Xvfb not installed — recipe in the doc).

Still open: interactive/stdin programs (vim/top/REPL) hit the 30s timeout — needs a real-PTY tier someday. Related: [[project_unattended_reboot]].

## Pigeons + the spool belong to PT (Nick 2026-09-17)

The zero-bitmap spool (storage/spool.rs — five-scalar VSF prelude, zero-window presence, ack-on-land, one finalize hash, on-demand window-hash repair) is NOT a pigeon-only piece: PT's ReceiveBuffer is a RAM Vec + a BitVec sidecar — exactly the redundant state the spool deletes — and PT has zero persistence, so transfers die with the process. Design recorded in docs/PT.md "Spooled receive": sealed-blob payloads (attachments, pigeons, history bulk) get spool-backed PT receive = disk-bound + restart-resumable; plaintext-framed VSF (offers) keeps RAM (structure may contain honest zero windows; small anyway). CONSOLIDATION IMPLIED: attach_manifest/attach_chunk/WANT-bitmap parallel reliability layer + pigeon should become PT streams over spools — ONE bulk transfer story. Pigeon wiring should be built AS this, not beside it. BridgePigeon announcement wire (bpn/bph/bps) + blob_store_file already shipped.

PIGEONS BUILT 2026-09-17 (stages 1–3 of the consolidation, docs/PT.md "Built 2026-09-17"): drop a file on a bridge conversation → `send_bridge_pigeon` streams it into the vault, announces a typed `RefKind::BridgePigeon` row (content = file name, `BridgeWire.pigeon` = name/hash/size), seals + dispatches `pigeon_chunk` PT frames under the FLEET key from the seal worker, sheds the local copy. Host: announcement opens a zero-bitmap spool (`network::pigeon::PigeonReceiver`, `runtime_dir()/pigeons`) keyed to the announcing device; chunks pwrite into slots; on the zero-scan saying whole, `take_complete` + `finalize_inflight` on the seal worker decrypt-walks, whole-file-hashes, `land_blob`s (never overwrites) into the sibling's shell cwd (`BridgeCwdMap`, from the shell worker's command sentinel; else home), sheds spool + vault copy, and answers the operator with a `BridgeOut` row naming the path (or `Msg::PigeonLandFailed`, 16 languages). Poisoned spool (completes to wrong hash) is shed, not resumed. **Field test PENDING** (cross-device leg compile+unit only). Load-bearing seam gotcha: the status handler in ui/photon_app/status.rs holds `checker` borrowed from `status_checker` for its whole length, so every arm uses FIELD-level borrows — a `&mut self` method call there is a borrow error; inline it. DEFERRED field-proven-first: stage 4 (PT `ReceiveBuffer` spool-backed for sealed payloads; attachments migrate onto it — attachments already resume via vault chunks, so it's risk without gain until proven) and the window-hash repair dialogue.

## Pigeon progress bar (2026-09-20, Nick: "we definitely need a progress bar when sending shit thru the bridge")
Built (commit 22fc708a): host answers each chunk's landing EDGE with a signed `pigeon_ack {tok, hash, got, of}` (protocol.rs, small-packet lane beside attach_have, reaches direct or off the pipe), thinned by `bridge::pigeon_ack_due` (first, last, every 1/64 crossing → ≤65 per pigeon; KAT). UI map `pigeon_progress: HashMap<hash, PigeonProgress{device,name,got,of,at}>` (photon_app.rs): sender inserts at drop (got 0), host at announce (resumed count), `on_pigeon_ack` takes max (siblings only), host arm updates from `PigeonReceiver::chunk()`'s new `Option<(landed,total)>` return; entry removed at whole (client) / landed drain (host). Row finds its bar by sibling device + `msg.content` name, newest `at` wins (the row carries the name, not the hash); bar shares the attachment bar's draw in render.rs (`bar_frac`). Perf fix riding along: `Inflight.present` RAM bitmap seeded from `spool::missing` at announce — `is_complete`/`take_complete` run the disk zero-scan only when RAM says whole (was after EVERY chunk on the UI thread). Field test PENDING like the pigeon itself. Tells: "PIGEON: spool open … N of M chunk(s) already held", "PIGEON: <name> — host <dev> holds every one of N chunk(s); landing next".

