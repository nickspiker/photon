---
name: project_manifestus_plow_reloc_refusal
description: CLOSED 2026-09-08 (manifestus f28e851 + photon banner reset) — "storage degraded" (amber) on Nick's phone = manifestus plow relocation guard refusing "relocation repointed nothing for block at 2078" ×3 during a history-restore write burst on a FRESH vault; guard is correct (no data lost), sick latch is sticky per process; root cause = plow live-set includes a block the index no longer references
metadata:
  type: project
---

Field 2026-09-08 21:11 (Nick's phone cacbc223, release v0.85.0, log tag 11c80922972e dev cacbc223): three vault stores refused within 6s — two `STORAGE: async message persist failed` + one `PHONEBOOK: persist failed`, all `Vault: corrupt: relocation repointed nothing for block at 2078 — refusing to orphan a live block`. Each failure calls `flag_vault_sick()` (messaging.rs ~296/454/500) → driver.rs mirrors it into `vault_degraded` → AMBER "storage degraded" banner. There is NO reset path for VAULT_SICK (grep: nothing stores false) → the banner is sticky until the process restarts even if every later write succeeds.
The guard: manifestus/src/hamt.rs `repair_relocs` (the "LOUD ON MISS (field 2026-08-24)" fix — moving anyway once turned 214 live blocks into danglings). Planner: the plow's step 3 "standalone block moves" (hamt.rs ~897-912): for each block in the live-set walk not in `delta.removed`, read+hash-check, append a copy, repoint the index; if repoint swaps nothing → refuse (window fails whole, originals stay live). SAME block 2078 three times ⇒ a persistent condition: the live-set walk counts 2078 live but no index route references it (a leaf/node superseded by a same-key rewrite whose old bytes are intact — the hash check can't see "index moved away"). Suspect: the live set is computed from a snapshot older than the index at repoint time, in the group-commit/put_batch path under a write burst (the vault was being filled by fleet history recovery: 68/91/111/197-row delta persists in the prior 15s).
Context that matters: the phone's vault was BRAND NEW at 21:09:37 — the log begins at process start with NO `ExitInfo: PRIOR RUN DIED` line, 0 contacts, sibling ceremony + fleet key recovered from the ira wrap + avatar recovered from the wall + 13 roster entries → contacts rebuilt Pending, 0 rows, history sweep armed for 13 conversations. App data was cleared before that boot (uninstall/reinstall or clear-data; ANDROID_ID unchanged so the device identity cacbc223 survived — the "statelessness invariant" working as designed). This morning the same phone held 410 rows for Emma. So: not caused by the era work (release build predates it); a fresh-vault restore burst tripped a plow invariant.
TODO: (1) reproduce in manifestus with a put_batch burst + plow on a fresh ring (KAT: live-set vs index agreement before each reloc); (2) make the plow re-verify index reachability at move time or recompute the live set per window; (3) consider clearing VAULT_SICK on N consecutive successful commits, or make the banner say "degraded earlier this session" — Nick decides; (4) ask Nick whether the phone was reinstalled/cleared before 21:09.

**ROOT CAUSE + FIX (same day):** `LiveSet::apply` (manifestus/src/vault.rs) applied `removed` BEFORE `added`. A block added and retired inside ONE delta — the same key written twice in one group commit (leaf L1 appended, rewrite retires L1, appends L2) — hit `removed` while L1 wasn't in the map yet (no-op), then `added` inserted it: L1 live, unrouted, bytes intact → the plow's hash check passes, the repoint guard refuses, forever. History restore re-persists the same conversation table per page ⇒ same-key-twice per commit is the JOIN-PATH NORMAL CASE. Fix = added first, then removed (correct because the tract is append-only within a delta). KATs `same_key_twice_in_one_batch_leaves_no_unrouted_live_block` + `put_then_delete_in_one_batch_leaves_no_unrouted_live_block` (tests/vault.rs) with the `live_map_divergence` test aid; on the old order both fail with the field's shape ("live map holds blocks the index cannot reach: [0..7]"). Photon: `clear_vault_sick()` on any successful persist after a failure + driver mirror clears a LATCH-raised amber (never a heal-at-open/dead-open one) + `StorageRecovered` toast. Field verification pending: Nick's next wipe+reinstall.
