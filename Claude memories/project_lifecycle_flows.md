---
name: project_lifecycle_flows
description: "Identity/device lifecycle DESIGNED in docs/lifecycle.md 2026-07-17 (flow tree, canonical screen names, flood conventions); punch list in TICKETS; NOT built"
metadata: 
  node_type: memory
  type: project
  originSessionId: 5e7ea89e-050d-402f-8393-9069280cb926
---

docs/lifecycle.md is THE canonical map of the identity/device lifecycle (@ d173bbf): state model (device × identity × session), full flow tree, screen names (Launch/Claim/Attesting/KnownHandle/JoinerWords/JoinerSelected/SponsorAdd/SponsorConfirm/Ready/Panel/LastRites), conventions (flood states: amber=dev, green=Selected, red=LastRites; the interstitial pattern = press-again-proceeds/anything-else-cancels; event-shown notices per [[feedback_no_time_based_ui]]).

The three defects it grounds (all observed live 2026-07-17):
- **D1 collision**: a handle collider derives the SAME identity seed (handle→seed), so ProbeOutcome::JoinOurs fires for strangers too — undetectable by construction. Fix = KnownHandle screen speaking to both readers (taken-first), and NO bind request/beacon until "It's mine" (today it posts immediately = collision looks like pairing + spams the owner's registry).
- **D2 double-attest**: one device attested two handles. Device keys are fingerprint-only (NOT handle-salted) so the worker's one-owner-per-device gate (exists in fgtw-bootstrap source, device_owned + ownership index) WOULD catch it — the live fgtw.org worker predates it. Fix = redeploy worker (standing-authorized) + client device-binding marker at vault_key("device_binding", device_pubkey) sealed under device key, consulted by the probe BEFORE spending the proof (DEVICE BUSY line on Launch). Marker cleared only by wipe, not takeover.
- **D3 last-device exit — SUPERSEDED 2026-07-17 same-day by [[project_identity_never_dies]]**: the free-on-zero + total-purge ruling was reversed and LastRites CUT (shipped): the worker refuses zero-member folds, the client refuses last-device sign-out, brands survive departure, owner-consent device_release frees hardware. The genesis-hash pin SURVIVES the reversal (custodian supersession + defensive rendering still need it); friend-side not_found = ENDED-but-frozen rendering also stays. Backup NFC tag = a $1 fleet member (phone mints keypair, signs both add-halves, flashes seed, zeroizes; bearer instrument, no passphrase).

Build order (TICKETS punch list): worker redeploy → binding marker → KnownHandle → JoinerSelected green flood → collision counter/notifications (LastRites dropped from the list; Security-page rework added per the never-dies ruling).

Also that session: scripts/lib/snapbuild.sh — dev.sh builds from a btrfs reflink snapshot (Code/.build-snap, stable path, dynamic path-dep closure from Cargo.tomls, real target dir shared, destroyed on every exit); edits during a build can't tear it.

**FLEET CALL LIFECYCLE round 1 SHIPPED 2026-09-08** (docs/calls.md §Fleet lifecycle): ring=breadth (offer express fans to every fold-trusted endpoint) / replies=precision (the ONE peer device: express src + its endpoints; validated_path fallback only); Offer/Answer carry device pubkey (optional 4th field, knows_device-gated, old rows parse None); drought tick (5s→reconnecting+Anchor express re-fires, anchor src re-points far TX via PEER_REDIRECT, 30s→honest "dropped" row); MUTE TRANSMITS ZEROS (wire-invisible, holds pinholes, never reads as drought); fleet_call_elsewhere chip ("wave in progress on <machine>", lit by fleet offer/answer rows, tombstoned by hangup/decline/busy rows). NOT built: join/switch handoff (crypto ready — sibling derives basket + advance_to(seq)), relay-carried anchor w/ reflexive addr (double-move heal), chip staleness on in-call device crash.

**EMMA'S GHOST CONVICTED + FIXED 2026-09-08 (the temp-device claim theft)**: census owner-fp instrumentation named it first pull — owner 002026e1 ("CaptainTenative") on BOTH her contacts = the device on her fleet for ~1h two weeks prior. Chain: legacy pre-§4.2 rows carry owner=None → temp sibling got contacts BEFORE chain replication landed (fleet-first gate covers PROBING, not replication) → claim-on-pickup stamped itself owner of every None row (belt-and-braces only guards Some+woven) → LWW roster push beat Emma's None → device departed, NOTHING swept the claim → hid 2wks behind chain_woven until the vault prune exposed it as "secured on <phantom>". THREE fixes: (1) has_siblings render guard (fleet-of-one can't be secured-elsewhere), (2) departed-owner sweep in fold reconcile (claim by non-member = VOID; chains-holder re-claims, else None), (3) legacy None-owner claim DEFERS while any fold sibling is alive (claim only when effectively alone). SEPARATE issue: her 1.5-month Brittany history died to the PRE-lap-guard vault bug (31 danglings, one LBA cluster) likely triggered by the temp-device-era write burst; recovery = re-clutch (armed) + friend history walk pulls Brittany's copy when she onlines. Nick fact: ANDROID_ID/SSAID stable across reinstalls, rotates on factory reset ONLY (cloud.rs comment corrected).

**SECURITY PAGE = LOCK / WIPE / RELEASE (Nick 2026-09-08)**: Lock (de-attest, vault kept) · Wipe (was Shred: vault nuke + sign out, STAYS in fleet chain) · Release (ONE pill, was two intent-predeclared departures: bilateral departure, intent 0 = APPROVER CHOOSES). The 2026-09-04 approver-side intent menu is now BUILT: leaver's Release sends intent-0 request → approver types words → choice pills render on the fleet row (new owner = countersign+release brand / desk = countersign, brand kept) → one tap completes. Legacy intent-1/2 requests complete as declared; commitment-less pre-words requests degrade intent-0→desk (conservative). depart_choice field parks the verified-words state; Esc clears.

**THE FOUR DEVICE VERBS (Nick 2026-09-08, FINAL)**: LOCK (self only, Security page — de-attests and NOTHING MORE; still a member, vault intact, fleet unaffected; handle wakes it) · REVOKE (was "Lock out": treat-as-stolen — fleet refuses it, fleet key rotates away, worker lock survives a wipe; membership permanent = refusal never removal. Available BOTH remotely on a Fleet row AND as a SELF-action on the Security page, which is the desk case Nick actually wanted — you shelve the device holding it. Self-revoke is REFUSED with no other live device, since only another device can Reinstate) · WIPE (was Shred: vault nuke + de-attest, stays a member) · RELEASE (bilateral departure: sign out + wipe + remove from fleet, ALWAYS frees the brand). REJECTED: remote *Lock* — Nick: "why would I want to remotely lock the device? Seems weird" (a benign de-attest has no remote use; the remote need is always revoke). Ordering bug fixed in revoke_device: rotate key + push worker BEFORE apply_locked_set, else a SELF-revoke tears down the session spawn_fleet_key_sync needs and leaves its own key live in the current epoch. The approver intent menu built earlier the same day was RETIRED (Lock owns the drawer ⇒ departures have one flavour).
