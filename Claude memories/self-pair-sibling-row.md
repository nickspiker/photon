---
name: self-pair-sibling-row
description: "2026-08-12 CORRECTED: no self-pair sibling contact exists — own-frame echo (LAN loopback + relay echo) poisons the REAL sibling row's endpoints/active_device with OUR OWN device; ceremony offers ourselves forever"
metadata: 
  node_type: memory
  type: project
  originSessionId: 81588914-5914-4600-bb98-72cc4fae2260
  modified: 2026-08-13T09:10:25.543Z
---

REVISED DIAGNOSIS (this supersedes the 2026-08-12 morning hypothesis, which log evidence disproved):

Disproven: "each device holds a sibling contact FOR ITSELF via roster merge". No `SIBLING: reconciled +1` with own device exists in any log; the census's three 7ff3835f tables are ALL legitimate — a9a00ab2 = notes (6 rows, present+loaded on BOTH devices), 3ad5fae4 = Mac↔Android pair (shared), 478dca5e / 5efe4e81 = each device's pair with 90e571bf, a third fleet member that is LOCKED OUT at the relay ("relay: recipient is locked out" — the stolen-device lockout, kept as member per [[self-only-removal]]).

The REAL wedge (field-proven, Android 2026-08-12 08:38:46): the sibling ceremony's answering offer loops back to its sender (LAN multicast loopback and/or relay echo — same class as Mac's ×701 "pong answered by 1be949c1 but we pinged c9b5f417"). The ClutchOffer receive handler resolves the frame to the SAME contact (same conversation token / shared hp 7ff3835f) and — BEFORE the "Ignoring duplicate offer (same keys)" check — runs the endpoint-adoption block: `contact.ip = sender_addr` (our own LAN addr), `active_device = sender_pubkey` (OUR device), `endpoint_mut(our device)`, `is_online = true`. Proof: the parked ceremony's relay fan-out is exactly `[1be949c1, fe46a74b]` — the Mac plus the Android ITSELF (own device in relay_device_list can only come from device_endpoints; sibling fleet_members stays empty by contract and the fold drain bypasses our own hp).

Consequences: (a) sibling ceremony sends offers to our own address → parses own offer → "duplicate, ignoring" → no KEM → parked forever, doorbell "7ff3835f ceremony parked" every ~5min since 2026-08-11 15:09 = "fleet sync not completing"; (b) presence lies ONLINE (we answer our own pings); (c) notes-to-self shows empty — 6 rows exist + load on both devices, but the UI binds a wrong 7ff3835f row (poisoned sibling → empty pair table). UI binding site unverified as of this writing.

Root bug class: NO receive path asks "is the sender US?". Own-authored frames (relay echo, LAN loopback) are processed as peer traffic — endpoints upserted, active_device elected, ip adopted, online marked.

FIX SHIPPED photon beee387 + FIELD-VERIFIED 2026-08-12 evening (v0.51.191): echo guard works — "relay: recipient is OUR OWN device — echo refused" fired on the first post-update ceremony attempt AND the 573KB offer was DELIVERED to the real sibling ("ClutchOffer delivered to 1be949c1"); fe46a74b's 7ff3835f doorbell went silent after 20:00. Fixes: drain guard (StatusUpdate::sender_device), relay self-refusal, answerable-set filter.

SOLVED 2026-08-13 (photon 4417b90, awaiting field verify) — the self-reading census named it in one round: 1861e459 IS the identity pid; the parked "sibling" ceremony lived on the NOTES-TO-SELF row. ROOT CAUSE: ContactId::from_pubkey = blake3(public_identity) collides BY CONSTRUCTION — the notes row (first-met device X, from roster) and the sibling row FOR device X share one id; the keygen drain's first-match-by-id installed the sibling's re-key onto the notes row, which then offered 573KB at our own fleet on the self-pair singleton token (5be16284 = derive over {identity_pid}), forever. The receiver's trust gate dropped those offers (zero-remote row knows no device) = "untrusted device for 1861e459". tok 6426b13d = the TRUE sibling pair {1411f260(1be949c1), 6288c031(fe46a74b)}. NOTE: fe46a74b is the MACBOOK, 1be949c1 is the ANDROID (JNI lines prove it — earlier sessions had them backwards).

FIXES 4417b90: sibling ContactId keys on sibling pid (domain-separated; loader re-asserts over pre-fix blobs; test pins non-collision); keygen results on zero-remote conversations discarded; boot scrub settles zero-remote rows Complete + clears round debris (silences the doorbell); census prints ContactId. Earlier layers all field-verified: echo guard (beee387) delivered the offer + silenced the Mac's doorbell; trust-gate token-row fix + census (0889d6e).

2026-08-13 Heathrow round (v0.51.198): collision fixes FIELD-VERIFIED — pair keygen ran under pid-keyed ids (contact_id 6288c031/1411f260), offers+KEM crossed, Mac reached PROOF stage. NEW deadlock found+fixed (5f1535a): Mac slept through hotel checkout mid-ceremony; Android restarted (rounds are MEMORY-ONLY — any restart mid-ceremony forces a fresh round), its fresh 573KB offer was relay-"delivered" into the Mac's dead pipe socket and vanished; Mac at proof-of-round-A vs Android awaiting KEM-to-round-B; NO re-send edge existed (stall re-fire required validated DIRECT path + excluded siblings; dozed doorbell needs 90s silence that relay pongs keep resetting). Fix: stall gate = Pending + offer_sent + (validated_path OR is_online) + no-peer-offer-in-slot, siblings included. Cross-round early proofs already discarded at derivation (18249) so the roll-forward is clean. Nick's stuck message = composed on chainless Mac → fleet-forwarded → waiting on this weave. STILL OPEN: (a) relay worker "delivers" into dead pipe sockets — no liveness check/mailbox, frames silently lost (worker-side fix); (b) Android pings locked-out 90e571bf (fleet.locked sync). Related: [[relay-asymmetry-ping-reflection]]. Related: [[split-contacts-incident]], [[lane-rotation-wedge-heal]], [[boot-blindness]].
