# Photon TICKETS

Working list. The fluor migration this file used to document is **finished** — all platforms share one UI (`src/ui/photon_app.rs` under fluor), the legacy stack is deleted, and every screen (Launch/attest, Ready/contacts, Conversation, Settings panel) is live. Conventions live in [AGENT.md](AGENT.md) (+ `../fluor/AGENT.md`); build via `./scripts/dev.sh`, never bare cargo.

Item format: what's wrong / what's wanted, then any scoping notes worth keeping. This is the ONLY working list — docs/TODO-later.md is dead and folded in below (2026-08-06).

---

## Current — stabilization week's parked items (2026-08-06)

- **Fleet epoch spine — B1+B2 SHIPPED, B3 leg one (2026-08-12)**: the chain and the eggs land on the fleet plane. The membership chain grows a CHECKPOINT op (kind 3, fgtw crate — chain-format flag-day, pre-checkpoint builds hard-fail the parse; a pinned pre-change vector proves deployed Genesis/Add/Remove signatures survive, the ckpt fields are kind-gated OUT of their signing bytes). Epoch 0 = spaghettify over the 2MB avalanche reservoir (CSPRNG eggs + fleet binders, pad+eggs dropped after — §14.3 one level up); epoch_k = spaghettify(prev ‖ settled_root ‖ fleet_key ‖ fanout_epoch ‖ k) so MESSAGES are key material and rotation is the PCS boundary. The public chain carries only blake3(root); the SECRET root rides sibling-to-sibling in ckpt_root frames sealed under the k−1 key (open-success = member-grade auth — no fgtw in the adopt path); laggards/joiners jump via fleet-key-sealed ckpt_state frames or the custody slot; local-root divergence logs CKPT DIVERGENCE and defers to the history sweep (repair = convergence, never a wedge). chain_sync frames now seal under fleet_epoch_seal_key(epoch_k, "chain_sync") carrying k (accept k/k−1). Mint edges: bootstrap (no spine → custody read, else mint + race k=1 on the chain via the worker's extends gate) and rotation (pending_fleet_key take arms the next sweep). FLAG DAY: fleet update + fgtw worker deploy together. REMAINS: (a) B3 legs 2+3 — sibling hist_page pushes + sibling pong tails still seal under fleet key / identity-seed keys, same re-seal shape (k field + epoch key + k/k−1 accept); (b) the every-256-settled-rows mint cadence (only rotation + bootstrap edges fire today, so trailing-K exposure G5 is rotation-bounded); (c) refold-time reconciliation of adopted epochs against the chain's commitments (defence-in-depth log); (d) custody currently rewritten only by checkpoint winners — a rotation with no subsequent checkpoint leaves the slot sealed under the old fleet key until the next mint (ckpt_state serves the gap).

- **Typing lag — FIXED pending field confirmation (2026-08-09)**: the probe named it exactly — phone Conversation renders averaged 21ms/keystroke (max 136, n=2460), a full-viewport re-raster per key. Compose typing (and Android IME commits) now skip `scene_dirty`; the box's own damage covers it (@a1b7653). Probe stays in for the verdict log. Same log round verified the rest of the 2026-08-08 batch: digest walk loop DEAD post-update (0 walks both devices), durable-then-signal live, CAS voids exactly the benign raced-duplicate case, no forks/resets/strand-misses. The unattributed 400-1700ms ticks were the spawn-per-item herd (fifty pages = fifty threads at launch) — nine sites now queue onto two immortal workers (photon-seal / photon-braid) and every drain names itself >50ms (@b725ed9).
- **Stuck lane at anchor — LANE ROTATION SHIPPED for the anchor case (2026-08-09)**: the 2026-08-08 "cosmetic-ish" call was wrong — the fresh logs showed the Nick→Mary direction FULLY dead on it (Mary at the anchor 8d6c9f5e, every Nick frame gap-buffered unlinkable, 2,913 retransmit lines overnight in the give-up → stall-recovery-re-arm → give-up loop, and Nick's fresh sends never rendering on Mary). Shipped: `our_lane_wedged_at_peer_anchor` (peer advertises tip 0 while our oldest EXHAUSTED pending doesn't link at the anchor — deterministic, local, edge-triggered on the sync-record pong) → `rotate_our_lane` (retire the dead lane + its pendings, mint a fresh label; receiver materializes it from the wire and links from ITS anchor — sender-only, no flag-day) → `resend_held_messages` re-serves every undelivered row thru chain_transmit at the ORIGINAL eagle_times, so row identity converges fleet-wide. REMAINS: (a) the nonzero-STUCK-tip wedge (mid-chain regression) is undecidable from osc heads alone — needs per-lane head HASHES in the sync records ("peer's expected-prev is a hash I can serve" beats "tip number moved"), the A5-adjacent upgrade; (b) Mary held rows Nick lacked-nothing-of yet NEVER fired the digest resync walk despite being 3 rows behind with pull machinery armed ("fleet sweep armed 1 conversation(s)" then zero requests all session) — why the walk never left the gate needs a log round with the heal in place.

- **Relay policy convergence — escalation, not promiscuity** (Nick approved 2026-08-06): this week promoted chat + ceremony frames to ALWAYS-relay as triage; the end-state is one rule, because the relay is a metadata oracle (every relayed frame shows the well sender/recipient/time/size — direct and LAN traffic show it nothing) and parallel copies of big payloads double exactly the traffic whose cost is material. Unconfirmable-and-small (ACKs, ClutchComplete, the weave probe — no failure edge exists to escalate on): parallel always, all sub-KB. Confirmable (chat, anything ACKed): direct-first on a trusted path, the FIRST retransmit (seconds) carries the relay copy. Large (offers ~573KB, avatars, history pages): never parallel — size cap, fast-fail direct then relay-only. Land only AFTER the Mary/Nick field round proves out on f6d1522.
- **Compose-anywhere adopt-edge unlock**: with lanes, a sibling that adopts a woven blob (lane_root present) can mint its own lane and send — seal `chain_woven` on the chain-sync adopt edge, retire "secured on <device> — send from there (for now)", then delete the fleet-forward fallback (lanes.md marks it dead code once proven). Queued behind ceremony convergence proving out.
- **Lanes leftovers** (docs/lanes.md status line): per-lane label rotation at re-key epochs; fork-on-a-single-writer-lane becomes a loud error (today the gap-streak repair stands in).
- **Lockout follow-ups**: an UNLOCK flow (deliberate act, not a merge artifact — nothing unlocks today); Fleet-page scroll (rows `take(6)` — devices past six are invisible); the verifiable single-report upgrade (a report carrying proof of current-epoch possession would let friends verify instead of trust).
- **Hit-id bookkeeping**: short term, one source of truth for the settings pill bands (reservation / dispatch window / stamp sites are three hand-synced numbers — the Lock-out pill bug); long term, press-captures-meaning dispatch (ids per-frame, release fires only if the widget under the cursor resolves to the SAME action — no reservations, no windows; content-hashing is OUT, the id space is 16-bit and birthday collisions would hit every user).
- **PT ladder vs offline recipients — FIXED (2026-08-09)**: the relay's "recipient offline" verdict now parks the whole ladder (`clear_outbound`); the came-online edge re-serves. Same pass DETACHED every relay HTTPS leg in the status-thread drains (chat/ack/history/PT-tick — the ping path had already learned this): the inline awaits serialised the loop at ~1.2s per trip, which is where the field's 5-10s ACK/reaction latency lived (ACK queued 07:27:04.8, dispatched 07:27:14.4 behind a retransmit spray). CLUTCH ceremony drains keep their awaits deliberately — rare, and the offer's relayed-count verdict is load-bearing.
- **History-request expiry vs relay latency**: the 45s in-flight timeout re-requests pages the relay eventually delivers (rid-unmatched drops observed in field logs). PARTLY FIXED 2026-08-09/10: the rid registry (below) now matches any request we minted, so a late copy merges instead of dropping.
- **SHADOW CONVERSATIONS — root cause found and FIXED (2026-08-11); history loss is real and partial**: the census closed the 2026-08-10 "messages don't recover" case. Three receive-side arms (braid commit, ChatMessage, MessageAck) materialized their conversation from `chains.participants()` while EVERY other path — loader, persist snapshot, page server, census, walk digest — derives thru the CONTACT (`[our pid, handle_hash]`). Whenever the chains' expression of our half differed (a stale-era ceremony's participant set), live rows accumulated in a SHADOW object no loader fills and no persist snapshot reads: rendered all session, DIED at restart. That was the device advertising 92 rows (shadow RAM) while serving 7 (contact table on disk) — and why every device now holds only 7-8 rows of that conversation durably: most of the wedge-era history was never durably written ANYWHERE and is gone. All three arms now resolve thru the contact, with a loud `SHADOW SEAM` line when the chains disagree (that line firing = a stale-era chains blob worth re-keying). Same census run caught the desktop holding THREE self-contact rows (the 2026-08-07 "CLUTCH offers toward its OWN identity" ticket): boot now PURGES empty self-stubs (wrong-key rows bearing our handle_proof, vault + index) and the roster merge REFUSES to mint them. WATCH next round: census row counts should grow session-over-session now; `SHADOW SEAM` lines identify remaining stale-era chains.
- **Restore round 2 (2026-08-11, same-day logs)**: the shadow fix PROVED in the field — one Nick device loaded 97 rows at boot (was 8), 109 by the next. Remaining: the behind device decrypted the same retransmitted frame every ~15s for ten minutes and never ACKed/persisted — its chains hold a STALE-ERA PARTICIPANT SET (neither our identity nor sibling pid listed) and the braid commit's party-id seam broke SILENTLY one gate before the shadow fix. Fixed: the seam now resolves the peer by matching participants against the contact list, loudly (SHADOW SEAM — blob should re-key). Also fixed: the digest kick and the friend route no longer require `chain_woven` (the page-seal capability is the history key; stale woven flags left the behind device's walk never arming at 8 rows vs an advertised 109); the census DUPLICATE detector no longer flags ordinary sibling rows (fleet shares one handle_proof by design — two rounds of false positives); census lines mark siblings. LAN-address hole root-caused and fixed: `get_local_ip`'s routing trick names the INTERNET interface (cellular CLAT/CGNAT on a phone) — the record's LAN slot now prefers the address observed on our OWN looped-back beacon (`OurLanAddrObserved`, kernel truth for the interface the beacon left), republishing on change. History that was never durably written anywhere (the shadow-era RAM rows) is unrecoverable; everything else should converge via the now-unblocked walk.
- **LAN DISCOVERY WAS DEAD FLEET-WIDE — FIXED (2026-08-11 evening)**: every device logged Multicast RX for every beacon and ZERO "Discovered peer" — `parse_lan_discovery` did a bare `VsfSection::parse` and checked `section.name == "pt_disc"`, but the section NAME lives in the header TOC (near-form), so the name came back `""` and every beacon was rejected, silently, everywhere. THIRD documented victim of this exact trap (after the relay pipe and the hub push accelerator — relay.rs even warns about it). Resolved via `header.primary_section` + a round-trip unit test that would have caught it the day the codec drifted. This was the deep root of the relay/direct asymmetry: same-LAN peers could only learn each other from registry records, which are WAN-only for a multi-homed phone. Same pass: a STRAND MISS now arms the full history walk directly (the sender provably holds our missing row — it just wove it), ending the decrypts-forever-never-ACKs loop for a history-lost device with no siblings; the digest-record path's remaining silent gates stop mattering for that heal.
- **Status-drain UI-thread stalls — BOUNDED (2026-08-11 evening), root profile pending**: the follow-up round showed 1.8s passes with ZERO arms over the 100ms timer — death by hundreds of moderate updates, invisible to a single-offender threshold. Shipped: a 250ms TIME BUDGET on the drain loop (backlog defers to the next tick; budget-deferred synthetic replays re-front chat_replay_queue so strict ordering holds) — the felt stall is now bounded whatever the storm — plus a per-pass PROFILE line (`PERF: status pass Nms over M update(s) — top arms: …`) logging cumulative per-arm time on any >200ms pass. NEXT: read the profile from the next log round and async-convert whichever arms it names (the 2026-08-07 leftovers — HistoryPage/Request, ChainSync — are the standing suspects, previously measured at 300-700ms singles under page churn).
- **Asymmetric relay/direct — PROBE REFLECTION SHIPPED (2026-08-11)**: path validation was one-directional by construction — a verified probe from a known device carries a proven-working source address, and the receiver used to ACK it and throw the address away. The side with good candidates validated; the side without (the field case: a peer whose published record carried no LAN address, so we probed only its unreachable-without-hairpin WAN) parked on "pending relay" forever while its peer's keepalive probes kept arriving. Now the receiver fires its own probe back at the verified source (rate-capped 60s/device, bogus-addr guarded, no new wire format) and the existing ack path validates our direction — either side knowing ONE working candidate converges BOTH. Residual (small): find why the record lacks the LAN address (Android local-addr detection publishing WAN-only; a `0.0.0.0` candidate was also observed in punch sets once).
- **Ghost decommission**: 90e571bf (pre-wipe home desktop) is locked out fleet-side and will be friend-refused on first report; the hardware still needs a physical wipe/kill when reachable.
- **Relay-pipe heartbeat efficacy**: watch field logs for `PIPE: silent past the liveness window` — proves the 45s ping / 120s reconnect is catching the silent-death case (one desktop sat 52 min dead-but-connected).
- **Field devices**: Daniel still on a pre-flag-day build — needs an update plus an online overlap to complete her re-key.
- **From the 2026-08-07 full log read-through** (three-log forensic pass; the serial-send + chain-first-dedup fixes shipped same day): FSTATE pull/push storm at boot hits FGTW rate limiting (error 10058, ~40 cycles in 14s — the "local roster ahead, pushing back" reconcile loop needs damping); the phone mints CLUTCH offers toward its OWN identity (two Pending self-contacts at boot) and blasts 573KB offers at seven known-offline contacts; desktop's chain_sync payloads fail to parse when they arrive over direct PT (10x "not CLUTCH" — same payloads parse fine via the pipe); "Status: active device  unreachable — rotating to fleet member X" logs an empty name and rotates TO the just-offlined device; ghost frames still cost a refusal each (~50/session — needs receiver-side backoff or pipe-level drop); ~~dev builds print the identity seed phrase in plaintext at attest~~ (FIXED 2026-08-09 @dec5e30 — the handle string logged on a failed chain fetch; the same pass scrubbed contact display names AND full decrypted message text out of the chat logs, all found verbatim in a submitted field log); ClutchKemResponse decap runs on the UI thread (~120ms x4 dup arrivals), HistoryPageReceived up to 467ms — the async-conversion ticket's live evidence; eagle_time minting is not monotonic per lane (probe 2 stamped 1.4ms EARLIER than probe 1).

## Fleet-assisted attest — "I hold the vault, my friend holds the key" (designed 2026-08-07; build once messages flow)

Auto-attest after reboot with NO durable identity secret at rest. The capsule stores identity_seed, whose handle derivation is deliberately CHEAP (`blake3(x(handle))` — handle_proof is the memory-hard one), so a stolen durable capsule falls to a dictionary sweep of human handles and then passes even the lock-out's handle gate: never seal it under a machine fact. Instead:
- Waking device stores OUTSIDE the vault: `wake.manifest` (handle_proof + sibling device pubkeys — public, addressing only) and `wake.capsule` (the 96-byte SessionIdentity sealed under a random `wake_key` the device does NOT keep).
- Each sibling holds `wake_key` in its own vault under the fleet key, keyed by the waking device's pubkey.
- Enable (offered only with ≥1 sibling; Android additionally only when `KeyguardManager.isDeviceSecure() == false` — on a locked device FBE blocks pre-unlock reads anyway and direct-boot storage is the weak store, so capability check = risk check): mint key, seal capsule, push shares sealed to each sibling's device key, require ≥1 ACK, zeroize.
- Boot: derive device keypair from the machine fact (survives reboot), read manifest, open the relay pipe (keyed by device pubkey — works vault-less), send signed WAKE_REQUEST to siblings over relay + LAN.
- Sibling grants iff: signature valid AND requester in current fold AND NOT in the locked set — then seals wake_key to the requester's device pubkey (asymmetric; requester has no vault yet), logs it, raises a fleet-inbox notice ("DeviceX woke at T").
- On grant: open capsule, attest, immediately ROTATE (fresh wake_key, re-seal, re-distribute) — a captured share is single-use.
- No sibling reachable → wait and retry forever, alive-but-unattested; manual handle attest always works. One-device fleets: physically impossible to enable (no share can exist) — the UI only reports it.
- Policy knob: auto-grant (default, the unattended property) vs require-approval (closes the "thief powers it on before you lock it out" window at the cost of unattendedness — user's call). Residual risk in the warning either way.
- Later: TPM/StrongBox/SE wrap of the LOCAL half for the headless-Linux node (the one genuinely good TPM use here); fleet veto stays on top. Related: docs/device-trust-and-recovery.md, the 2026-08-05/06 lockout stack.

## Identity profile — stage C and beyond (folded from TODO-later, 2026-08-06; home doc: docs/identity-profile.md)

- Stage C (the visible half): worker `profile_put`/`profile_get` at `profile/<hp>` (identity-signed envelope verified against chain genesis, opaque blob, ~4KB name-only v1, hub `profile` event); client blob v1 (name under a random profile key; grants sealed to each friend's pinned identity pubkey at `blake3(recipient_party_id ‖ hp ‖ "grant")`); publish on name-set + friend-add, fetch on contact-fleet-refresh, adopt into `published_name`; Settings Profile page with the load-bearing "you don't have to fill in ANY of this" note. Until C lands, contacts render as keyed voca pseudonyms.
- Beyond C: extended profile card (per-field keys, per-contact share checkboxes — merges with the Per-field disclosure item under UI/UX); profile key epochs + ostracism UI; grant-carried friendship salt (per-relationship revocability of the CLUTCH secret); avatar epochs ride field keys once grants land; NFC invite card (rides pairing-v2 NFC). Petname editor is DEAD — Nick removed the concept entirely (PRST5).

## Pairing / fleet (folded from TODO-later, 2026-08-06; home doc: docs/pairing-v2.md)

- NFC tap transport for device-add (HCE; candidate machine built, tap delivers, touch selects); BLE transport (lock word + proof beacon; shadow beacon proven).
- Self-departure UI: Security-page "Remove this device from fleet" stub — `depart_device` exists, unwired. (The other half of this pairing, LOST-device handling, SHIPPED 2026-08-05/06 as the treat-as-stolen lockout: fleet-synced locked set, handle-gated confirm, key rotation, friend-side refusal.)
- Remove the two v0→v1 flag-day supersession branches (worker `handle_fleet_op`, client `ensure_member`) once no v0 chain plausibly remains; drop pairing-v2's retired word-list leftovers (`words_to_pair_pubkey`/`pair_entry_complete`, no production callers).
- **B4 fleet CRDT repairs — ALL SHIPPED (2026-08-09)** (adopt-echo kill + HealLatch 2026-08-08 @aa8ccfd; the remainder same-pass 2026-08-09):
  - `fleet.locked` / `fleet.released` union-merge race (SECURITY): per-key entries now — `fleet.locked.<hex pubkey>` (value = the raw 32 bytes), so concurrent locks of different devices COMMUTE under `merge_global_settings` instead of racing one LWW blob; the legacy blob unions in read-only forever (`FleetSettings::pubkey_set_union`) and is never written again. Race pinned by test (two devices, same stamp, different locks — both survive on both sides).
  - `merge_rosters` tie-break: equal-stamp entries fall thru tombstone-first to a canonical content order (`roster_entry_canon`) — the merge is commutative even for same-instant writes of different content (fgtw, test pinned).
  - fstate convergence backstop: an exhausted roster pull now re-arms (2 attempts) on the 45s fleet-refold edge instead of waiting for a fleet event that never comes with the WebSocket down.

## Blinding flag-day debris (2026-07-14, folded 2026-08-06)

- `photonlog` CLI still derives a submitter seed from a typed handle — dev tooling; a party-id flag someday.
- `search_status`/`SearchResult` carry the typed handle transiently for the "added {h}" toast — in-RAM only; could switch to the pseudonym.

---

## Identity / device lifecycle — see [docs/lifecycle.md](docs/lifecycle.md) (the canonical flow tree, screen names, conventions; DESIGNED 2026-07-17)

Punch list from the doc, in order:
1. Redeploy the fgtw.org worker (one-owner-per-device gate + index are in source but the live worker predates them — this is what let one device double-attest two handles). Verify with a scratch two-handle genesis.
2. Device-binding marker + Launch DEVICE BUSY line (client-side one-handle-per-device gate, fires BEFORE the proof is spent).
3. KnownHandle screen (the collision flow): "This name is already claimed" with both readings — pick-another-name first, it's-mine → pairing words; no bind request posted until "It's mine".
4. LastRites red-flood interstitial: last-member detection on Depart + Remove & shred; allowed but ceremonial ("End it — forever"). Ships WITH: the client-side cleanup sweep (avatar blob via pin, submitted logs via tag), the worker purge-on-zero-fold (all hp-keyed traces deleted, name frees first-come), and the genesis-hash pin (friends pin the genesis op hash; different genesis = stranger — the no-counter successor gate). One unit: free without the pin gate is the inheritance bug.
5. JoinerSelected green flood + sponsor-confirm hold (same flood mechanism as 4).
6. Collision counter + notifications toggle on Panel→Fleet (bind_attempt alerts already flow).

## Messaging / protocol

- **Message-format rework — remaining string markers** (2026-08-09): the reply/edit/react family SHIPPED fully typed same day (`ChatMessage.reference`: a second `(ref: u3 kind, e6 target)` VSF field on the wire, `ref_kind`/`ref_ts` row-record fields, `m_refk`/`m_reft` page columns — content carries only the body; the string-marker era never deployed). Still string-encoded because their rows/wire ARE deployed: the DELETE marker (`prefix + decimal ts` — its converter needs dual-parse forever), the chain-probe marker (a pure sentinel — a magic value, not an encoding), the attachment record-in-content (hex hash + decimal size), and the `profile._custom` registry (`id\tlabel` lines in a settings value). The delete marker is the natural next `RefKind::Delete` (a referencing row with empty body) — needs the dual-parse bridge and a tombstone-flag reconciliation, so it rides its own pass.
- **Friend consent gate**: `on_search_result(Found)` auto-adds + auto-CLUTCHes — search IS befriend. Needs a pending/accept state so a stranger can't complete a ceremony + deliver a message just because someone searched a handle (observed live 2026-07-16: "an attacker just got this message"). Mutual-consent design notes in the peers-are-fgtw plan.
- **CLUTCH ownership evolution (§4.2)** (design, 2026-07-31 — from the live dozing-owner stall: peer_m's re-key waited on Nick's sleeping phone while peer_b parked): two stages. **A — transferable claim (do first)**: ownership becomes a LEASE — any awake sibling may claim an unclaimed-or-expired ceremony; claims carry (epoch, device_pubkey) monotonic tiebreak and ride the fstate hub push (~150ms fleet-wide), owner renews while alive, ~30s silence → any sibling re-claims and drives. Friend side already accepts any folded member's offers; no wire change; deletes the dozing-owner stall. **B — race-and-converge (after the primary registry lands)**: no ownership; ceremonies are device-pair-scoped (ceremony_id already derives from offer provenances); ANY pair may run one; convergence at ADOPTION: both fleets saw every competing ceremony_id, so both independently keep min(ceremony_id) and discard the rest — late/duplicate completions are no-ops by construction. Cost: a message can ride a losing chain in the near-simultaneous window; the fork detector heals it, but that makes fork-healing a NORMAL mechanism rather than an emergency one — decide that deliberately.
- **CLUTCH sibling-race**: a peer's sibling device initiating CLUTCH before we fold it into the contact's fleet gets rejected by the PT gate and gives up before the fold lands (~20 min stall observed). Fix = re-initiate after fold, or queue the offer until the fold verdict.
- **Blind ops (private identity S) E2E**: the party-id token seam is fixed (2026-07-16, tokens now derive from party ids on both ends) but the full S lifecycle (probe → generate → deposit → ack → recover) hasn't run E2E across real devices since.
- **Fleet message sync E2E**: phase-2 sibling history sync + live push shipped @ c6d6285; needs the 2-device live run (send on A, watch B; fresh-join backfill; delivered-tick propagation).
- **NAT traversal relay tier**: reflexive/punch/keepalive exist, but symmetric↔symmetric NAT pairs have NO fallback path. Needs a relay tier (friend-fleet devices as relays fits the peers-are-fgtw direction).
- **Chain-advance desync (parked)**: one observed case of ratchet divergence by msg 2 after a Complete (garbage plaintext); salt-from-last_plaintext a prime suspect. Re-test post party-id fixes before digging.

## One-directional-path doctrine (2026-07-31)

Three independent subsystems died the same death in one day of live logs: a VALIDATED or plausible direct path whose REVERSE direction silently drops — blind srv answers (probe expired forever while the peer served every 15s), message ACKs (direct ack lost → sender retransmit-timeout → duplicate → only the re-ACK's relay copy landed), history page responses (2,322 re-requests in one session while every request arrived fine). Doctrine adopted: **small idempotent response frames ALWAYS carry their relay copy alongside any direct leg** — blind (all four frame types), ACKs (both sites), history page serves (one-device relay_to). Validation proves OUR probes round-trip, never that the reverse delivers. Suspect the same mechanism in punch-probe replies (why validated paths are scarce) — next traversal investigation.

## ~~Async conversion — the remaining UI-thread work (2026-07-31 inventory)~~ RESOLVED 2026-08-08

Landed as three commits, a different shape than the owner-thread design sketched here — the UI keeps ownership and workers get SNAPSHOTS, with CAS commits guarding the fork class one-sided off-threading invites:
- **f548560 (the seal room)**: every fleet-plane open/seal off-thread — history-page open (the 210-485ms arm stall), chain-sync open+decode, replication encode+seal, live-push page seal, attach-req blob serve, plus the per-page cursor persist onto a coalescing 13-byte writer. Drains re-gate against current state; nothing decided at dispatch is trusted for a commit.
- **f2eede5 (durable-then-signal)**: the two per-message commit-point saves ride the coalescing chains writer with their gated signal ATTACHED — the writer fires the receive's ACK / the send's transmit only after the durable write lands. Persist-before-signal holds exactly; coalescing carries a superseded snapshot's signals onto its replacement. Unread bump + clear join the conv-state writer.
- **fdee1fc (the braid)**: receive decrypt and send encrypt on workers. Receive: arm gates → worker decrypt on the lane snapshot → drain re-gates + CAS (expected-prev still verifying = lane unmoved = salt inputs current, so parse-garbage past it IS fork evidence) → the full commit pipeline; gap-buffer serialization comes free. Send: prepare_send split into pure encrypt (&self) + CAS commit (lane-key compare; a miss voids the ciphertext, mutates nothing, and the held-row sweep re-encrypts at the fresh position); one encrypt in flight per friendship.

Still inline, DELIBERATELY: CLUTCH KEM decap (~120ms, once per ceremony — entangled with the offer/KEM/complete state machine; move it when the ceremony code is next open, not as a drive-by), the two once-per-ceremony chains saves (CLUTCH completion, sibling chain reset), and startup vault loads (pre-first-paint).

## Transport / NAT traversal — direct-path hardening

Ordered; land + 2-device-test each before the next, relay tier (above) is last. Diagnosed from peer A (cellular carrier behind CGNAT) ↔ peer B (fiber) logs: peer A never held peer B's public candidate + no keepalive → one-sided punch (peer B 209 probes, peer A 0). DONE on branch `transport-resilience`: **validated-path keepalive** — clamp the presence sweep to 20s while any validated path is held so CGNAT mappings don't die mid-session (`photon_app.rs` `presence_ping_interval` + `VALIDATED_PATH_KEEPALIVE`, @5f5bfdc); **punch-on-address-learned** — `refresh_contact_addrs_from_peers` fires an immediate sweep when a peer addr is learned/changed, so the late-fetch side punches at once instead of sitting silent (@b5cc6c2).

- ~~**Address discovery via peer gossip (T0)**~~ LARGELY LANDED 2026-07-31: the seed now serves a per-record phonebook registry (`pb_put`/`pb_get`, fgtw-bootstrap; records self-signed, monotonic-timestamp guarded) and clients both publish their quorum-corroborated reflexive address to it and resolve stalled contacts' devices from it (`phonebook_client.rs`, `resolve_stalled_addresses_from_seed` — asked LAST, after gossiping every reachable peer). Verified live: resolve → endpoints → punch fired on both peer_a and peer_b the same day. Still open from the original write-up: sibling-gossip as the middle tier (ask a reachable sibling before the seed), and retiring the 15s full-resume pulse (it re-runs the whole attest; should be announce-only or doorbell-driven). Original ticket for reference below.
- **(superseded T0 text)**: address discovery is 100% dependent on a device's OWN fgtw.org reachability. On flaky cellular that strands everything downstream — observed peer A↔peer B: peer A's fgtw failed 4/7 (`contact fleet refresh failed for <dev>: No connection to FGTW`), so it never learned peer B's address, the CLUTCH offer was built + `Stored local offer` but NEVER sent, and the ceremony looped Pending re-keygenning every ~1-2 min. Meanwhile peer A had a healthy keepalive-held v6 path to its own sibling `<dev>` — which can reach fgtw AND knows peer B's address. Fix: on an fgtw fetch/refresh failure, ask a reachable sibling/friend for the peer's `PeerRecord` over the working path (`PhonebookRequest`/`Response` already carry self-signed, relay-verifiable records — the plumbing half-exists). Removes the single-point dependency on a device's own uplink. Higher priority than T1.3/T3/T2 below: those all assume you already HAVE the peer's address; this is why you often don't. Stopgap: aggressive fgtw retry (every ~3-5s) while a ceremony is Pending, to catch a working window sooner than the current ~1-2min cycle. NOTE: the fire-on-address-learned fix (@b5cc6c2) can't help here — it needs a learned event that a failing fetch never produces.
- **TCP as a direct path (T1.3)**: `tcp.rs` has NO inbound accept today — TCP is only a whole-payload PT fallback to an already-known UDP address (`pt/mod.rs` ~554). Add (a) an inbound TCP listener on `PHOTON_PORT` handing framed VSF to the same `status.rs` receiver pipeline, and (b) a parallel TCP-connect to a peer's public/forwarded candidate raced alongside the UDP punch. Wins: UDP-blocked networks (corporate / some mobile), and TCP-direct to a forwarded host "just works" (the port-forward / RustDesk-host case). Test: UDP-blocked client reaches a forwarded host over TCP.
- **IPv6 as a first-class direct path (T3)**: `gather.rs` prioritizes `HostV6` (100) but "full local-interface enumeration deferred (P2)" and the announce may not carry a global v6. Add local global-v6 enumeration + carry it in the FGTW announce (worker parse is already generic) so a v6-capable pair connects direct over v6 — NO NAT, no punch. Biggest single win for a cellular peer if its carrier is v6 (bypasses CGNAT entirely). Test: two v6 hosts connect with zero punch; verify a cellular device gets a global v6.
- **Server-coordinated simultaneous-open (T2)**: today each side punches on its own ping cadence with no two-sided timing, and the WS channel (`wss://fgtw.org/ws`) is broadcast-only. Add targeted WS delivery in the worker (per-connection subscription by device_pubkey / handle_proof) + a "punch-request" msg: A deposits "punch B, here are my candidates, window ≈ T" → worker pushes to B → B punches back at once. Breaks the chicken-and-egg AND gives tight timing for restrictive (non-symmetric) NATs. **NEEDS WORKER DEPLOY** (`fgtw-bootstrap`, user-run). Test: cold pair (neither pre-punched) connects on first attempt.
- Then **NAT traversal relay tier** (see Messaging/protocol above): the symmetric↔symmetric / double-CGNAT case (two cellular peers) that direct can never reach.

## Logging

- **Log trim audit** (downgraded 2026-08-01, was "CONFIRMED trim bug"): RETRACTED — Nick wiped peer_b between the purge and the submission, and a wipe deletes photon.log.vsf with the rest of the app data, so the 16-second file was sixteen seconds OLD, not sixteen seconds SURVIVING. Every datum now fits no-bug: peer_m's .40 submission spans 4+ hours (appending works), peer_b's post-wipe file spans exactly wipe-to-submit. The seconds-vs-oscillations audit of trim_log_file is UNPROVEN speculation until an honest measurement exists: a device with >24h of uninterrupted history submits, and the span is checked against the 16 MiB / 24-48h window. Do that measurement before touching the trim math.

## Updates

- ~~**Android update NOTIFICATION / unattended install**~~ MOSTLY DONE 2026-07-25: the tap-to-install now rides a PackageInstaller SESSION with USER_ACTION_NOT_REQUIRED (@f2b21b9) — one system confirm to become installer-of-record, silent forever after (Android 12+). Remaining: wire the AUTOMATIC path to auto-download+install (today it toasts, then a tap installs silently) — GATED on unmetered/wifi detection first (metered-data safety; the updates-page label says "Check" not "Install" on Android until then).
- **Push doorbell for releases**: the ~6–8h poll stands in; the designed push is a release notice riding the fleet inbox / hub events (docs/fleet-inbox.md — bind-attempt alerts already flow thru it, so the plumbing half-exists).
- **Rollback**: keep `.prev` and auto-revert after N failed starts (the Windows `.old` shuffle is halfway there).
- **Idle gating + in-flight textbox hand-off across the re-exec**: auto-apply currently swaps + re-execs whenever the gates clear; a mid-typing swap loses the compose box.
- **Heal the live dev manifest**: Android row claims 0.36.12 but carries the 0.36.11 APK; Windows claims 9 with (likely) a v7 exe — mis-stamped by the publish race fixed @ 58bf7ed. Re-run `scripts/publish/dev-android.sh` + `dev-windows.sh` (user-run; bumps + stamps correctly now).

## UI / UX

- ~~Friend avatar-change refresh~~ CLOSED 2026-07-17: the pin now ROTATES on every avatar set (fresh key+lookup, old wall slot deleted after the new upload lands) — friends see the new pin on their next pong and refetch; siblings ride the profile.avatar_ts ding. Rotation also self-heals any cross-identity pin pollution (the cross-identity-avatar incident).
- ~~**Connection-flow narrative revision**~~ DONE 2026-07-31: the ladder now narrates the exchange (creating 8 key pairs → sending/waiting public keys → locking/waiting secrets → combining → proof exchange → confirming → testing → secured); every "waiting" step names whose side the ball is on. Still open from the fold-in: quieting the proof-echo ping-pong ("Re-arming proof resend" — bounded but noisy, re-fires per relaunch).
- **macOS notification click launches Script Editor** (Nick, 2026-07-31): `desktop_notify.rs` shells to `osascript display notification` — zero-dep by design, but macOS attributes the banner to Script Editor, so clicking it activates THAT. Fix = post via `UNUserNotificationCenter` from the .app bundle (it exists now at ~/Applications, stable identity): objc2 delegate class, requestAuthorization on first notify, `didReceiveNotificationResponse` → the existing show-window/activation path from the Dock-reopen work (fluor macos_reopen). Interactive-only to verify (a human must click), so land it during a live mac session — do NOT ship blind (the Dock-fix SIGABRT lesson).
- **Attest wrong-handle UX** (Nick 2026-07-31): a device bound to handle A, attesting handle B, still walks the mint/permanence interstitial and only fails at the worker's one-owner gate with a generic message. The LOCAL marker gate (device_binding, pre-probe) works but legacy devices lacked the marker until their next resume rewrote it (bind() is idempotent on resume — self-heals). Remaining: (a) map the worker's one-owner rejection to "This device is already bound to another identity — type its handle to resume, or wipe first", skipping the generic failure; find the worker's err reason label near lib.rs:2795 and match on it client-side; (b) after that error, the blinkie + textbox are dead until click-off/click-on — the Error path's refocus_handle_select_all leaves focus state desynced; reproduce and fix the re-arm.
- **Contact-name sync** (Nick 2026-07-31): RESOLVED in two acts. Act 1 (PRST4, v0.51.56/.57): the displayed name is `published_name` (the friend's own pong-adopted choice); it lived only in the device-bound state blob while `avatar_pin` rode the roster — now it rides the roster too (pong adoption bumps roster_updated, post-drain sweep pushes on name OR pin adoption, merge applies it empty-guarded), so a re-attest/new sibling shows real names as fast as avatars. Act 2 (PRST5, v0.51.58/.59): Nick ruled the petname concept (a local label you give a friend — never had UI) a misunderstanding and had it REMOVED entirely: `Contact.petname` gone, roster name slot gone, index + cloud rows drop the name column, render order is published name → keyed pseudonym. Flag-day note: the slot carries names only after one updated device pushes post-PRST5. Residual (small): verify the hub fstate event triggers an immediate pull+merge on siblings (subscription exists; confirm the handler pulls rather than waiting for the poll).
- **Textbox glow on search state**: legacy tinted the search pill yellow-during / green-or-red-after a search; fluor's glow is focus-driven — recolouring per state needs a small fluor affordance (`set_glow_colour`?) + photon wiring.
- **Party colours are placeholder**: swap to perceptual L≈50% via vsf spectral/LMS (decided; supersedes the old Fibonacci-sphere colourize sketch this file used to carry).
- **Contacts search-box focus glow damage (parked)**: stale glow lingers/clips on deselect — bg pass dirty-gating skips the glow bbox.
- ~~**CLUTCH ceremony UI-thread hitch**~~ RESOLVED 2026-07-31: expansion was already on a min-priority worker; the REAL measured stall was `save_messages` (full encrypted table rewrite) running inline in the ChatMessage/MessageAck arms — up to 5.7s on the UI thread, named by the new per-arm PERF instrumentation. Message persistence now rides one coalescing background writer (@49b2b65). Any residual arm >100ms names itself in the log (`PERF: status arm X`).
- ~~**Avatar encode off-thread**~~ ALREADY DONE (stale ticket): `set_avatar_from_file` runs decode + Lanczos + rav1e encode + vault save + upload on one low-priority worker; display pixels return via `avatar_set_rx`. Nothing runs on the UI thread.
- **Wipe-recovery must restore the WHOLE contact, not a pin-set** (Nick 2026-08-01): the cloud blob is the only thing a wiped device gets back, so whatever it omits is GONE — proven twice in one day (contacts returned with avatars and no names; before that, an avatar orphaned by a re-minted pin). It currently carries `handle_proof, party_id, avatar_pin, published_name, device_pubkey, trust_level, added`. It must carry the full received-disclosure struct per contact — every field they shared with us (address, socials, whatever) with its per-field LWW stamp — AND our OWN profile/credentials, so a wipe restores the identity as it was rather than a husk. Same struct the per-field disclosure work defines below; build them together, and treat "does a wipe restore this?" as the acceptance test for every field added anywhere.
- **Per-field profile disclosure** (Nick 2026-07-31, spec sharpened): the whole per-contact options struct is UNBUILT — today the pong's sealed tail carries exactly two slots (display name + avatar pin) and nothing else crosses. The model: (1) You-page fields (first/middle/last/addr/…) are human text, VSF type `x`; each gets a CHECKBOX = "share by default with new contacts", default unchecked. (2) Display name is the exception: no checkbox, public, always shared, the ONLY non-optional field — always defined, type `x`. (3) Per contact, each field toggles on/off individually — on = shared AND kept updated on edits (the default checkbox only seeds a new friendship's toggles). The per-contact toggles live on the CONTACT PANEL (the chat page's settings): checkbox + the field's current value as NON-editable text — values edit on the main settings You page only; main-settings checkboxes = default behaviour, contact-page checkboxes = sharing with that contact specifically. (4) Recipient side: a per-contact struct where every field is optional (absent = not shared with us), display name mandatory — this is where their card renders from. Existing hooks: `ProfileField` (no share flag yet), `profile.<id>` settings keys, the pairwise pong-seal tail (the natural transport for granted fields), docs/contact-system.md (the fuller crypto design — per-field keys/grants — that this UX rides on top of).
- **Post-attest multi-device prompt**: right after a successful attest, prompt to add a 2nd/3rd device (redundancy IS the recovery story) + reflect in a Security/Recovery posture strip. Handle-loss warning itself is DONE (the `LaunchState::Confirm` permanence interstitial).
- **Profile rework (D)**: ONE key per base (`profile.addr`), instances = multi-value rows, identity = TAG (home/work/custom); kills `profile._custom`/`addrN` keys. Held/gated.
- **Updates-page checkbox label on Android**: reads "Install updates automatically" but Android can only notify — label should say so there.

## fluor-side

- ~~**Italic text**~~ DONE (faux-italic): `TextStyle::shear()` exists and the draw path threads `style.shear`; the pending-contact label uses `.shear(0.2126)`. True Style::Italic faces were the heavier alt and aren't needed.
- ~~**Colour emoji + monochrome symbols**~~ DONE 2026-07-25 @ fluor 4f8352a: Noto Color Emoji embedded (+10.8 MB), swash Content::Color RGBA-glyph blit branch folded into the darkness/under() domain; Noto Sans Symbols 2 already covered snowman/arrows/dingbats. No system-font reads (deterministic-bytes doctrine).
- **Android multi-touch**: single-touch works; pinch-zoom (and the two-finger zoom hint) waits on a multi-touch `Touch` event in fluor's android host.
- **Wayland drag-and-drop** (avatar upload): winit has no `HoveredFile`/`DroppedFile` on native Wayland (winit #1881 / PR #4504). Wait for upstream or a `wl_data_device` impl in fluor.
- **macOS multi-monitor: window disappears on cross-monitor drag** (PINNED 2026-07-25, real-hardware test): dragging the window toward a second monitor vanishes it. Phase B/C spawn per-monitor surfaces + route by WindowId; suspects: the second surface never spawning on macOS (available_monitors timing), the involved()/dormancy flip hiding the home surface, or points-vs-pixels in the straddle math (`pixel_ratio` only on macOS). Needs a mac debug session; Linux single-monitor is unaffected.
- ~~**Linux black-taskbar**~~ FIXED 2026-07-25 @ fluor b2b8907: real root cause (found live via xprop) = Muffin auto-promotes the undecorated monitor-sized surface to legacy FULLSCREEN state → fullscreen-layer stacking buries the panel. NOT unredirection (bypass-compositor hint + the unredirect gsetting were red herrings; the gsettings flip was reverted). Fix = surface height−1 on Linux; verified live: state stays FOCUSED, taskbar survives, photon gains a taskbar button.

## Platform / misc

- ~~**Android session capsule** (de-attest-on-restart)~~ CLOSED 2026-07-25 @ c40a3f2: real root cause was the wairua's boot secret reading /proc/sys/kernel/random/boot_id, which Samsung SELinux blocks from untrusted_app → boot_secret() None → session never persisted → re-attest every app restart. Kotlin now derives the boot secret from Settings.Global.BOOT_COUNT (per-boot-stable, no permission, every ROM) + pre-API-24 per-install-random fallback, handed to Rust via nativeNetworkInit before any session read. Survives app-restart-within-boot; re-attest only on genuine reboot.
- **TOKEN session relay** (Android sticky-broadcast gossip across TOKEN apps): protocol spec'd — every TOKEN app re-broadcasts every TOKEN session sticky, `PACKAGE_FULLY_REMOVED` triggers the survivors to re-fill the gap, signature-level permission gates participation. Photon's send/clear/restore side is wired. **Deferred until a second TOKEN app exists to test with.**
- **Chrome downloads on Android** (website): serve the APK so Chrome offers install, not a mystery download; or rename to `.zip` + extract instructions. Website-side.
- **macOS softbuffer present-on-clean**: legacy carried an untested "re-present even when clean or the window goes black" workaround for transparent windows; re-verify against fluor's renderer on a real Mac.
- **dev-adb.sh stale rust builds**: the adb dev deploy sometimes reuses a stale-built .so — force the rust rebuild or hash-check before packaging.
- **Android API 26-29 IME inset**: with `adjustNothing` (2026-07-25 keyboard model — surface never resizes), `WindowInsetsCompat.ime()` is only reliable on API 30+; pre-30 devices report 0 so the compose bar stays under the keyboard. All fleet devices are 30+; revisit only if an old device joins (fallback: popup-window height probe).

---

## Done (recent — see git log for the full trail)

- 2026-07-16 night: update stamp window (`floor < t ≤ now`, nunc tiebreak) + automatic release-channel updates (jittered ~6–8h, `updates.auto`-gated, desktop release builds self-apply, dev/Android toast) @ 228f68c · publish race fixed (pinned stamp + flock) @ 58bf7ed · blind-ops party-id token seam fixed · contact-row hover highlight · "+"-button press flash suppressed · Ready-layout handle slot collapsed (search box sits by the avatar) · EXIF orientation applied in the avatar decode · last legacy file (`ui/colour.rs`) deleted.
- 2026-07-16: fleet message sync phase 2 + roster CRDT @ c6d6285 · theme extracted to `ui/theme.rs` (fluor format) @ aa1ca3c · dozenal versioning + manifests + manual two-button updates + avatar friend-gating (ChaCha20) + per-device addressing + structured logging (earlier in the week).
- Long-since stale claims this file used to carry, all verified live: Conversation screen (bubbles, send button, Enter, ordering via sorted inserts + numeric key sort), row click → conversation, not-found select-all, auto-clear search on add, `Textbox::clear`, fleet-inbox v1 bind-attempt alerts, handle-loss warning (permanence interstitial), send-button overlap, message notifications on desktop (chirp chime; Android platform notifications still open above).
