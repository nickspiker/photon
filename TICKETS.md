# Photon TICKETS

**Working list — OPEN work only.** A shipped fix leaves this file: its commit message carries the mechanism, the evidence and the reasoning, permanently, addressable by hash (`git log --grep`). What survives a fix here is only the *residue* — the part still undone.

Entry shape: what's wrong / where the evidence is (date, device, file) / the next concrete move. Long forensic prose belongs in the fixing commit or in `docs/`, not here.
Conventions live in [AGENT.md](AGENT.md) (+ `../fluor/AGENT.md`); build via `./scripts/dev.sh`, never bare cargo.

---

## NOW — bleeding, or next up

- **Ghost sibling 1be949c1 holds EVERY friend keygen hostage (CONVICTED 2026-09-01, desktop + phone logs)**: both devices loop `CLUTCH: Sent offer to 7ff3835f` → `ClutchOffer to 1be949c1 did not land: recipient offline` → `offer reached NOBODY — waiting for the peer to wake` → **`fleet-first — holding 8-9 friend keygen(s) until every sibling is probed`**. 1be949c1 is the PRE-re-attest phone device — permanently dead — so the fleet-first sibling-probe gate never opens and friend ceremonies queue forever on those devices ("Theresa's clutch can't complete" was this from the non-completing side; her ceremony actually completed via the desktop responding to HER initiative, no local keygen needed). The de-fold reconcile never dropped the ghost because the sibling contact predates the era split. FIX (two layers): (1) reconcile must also drop a sibling contact whose device key is absent from the CURRENT fold even when the contact predates the fold flag (the era-debris sweep); (2) the fleet-first hold must be bounded — a sibling that is de-folded OR unreachable through N probe cycles counts as probed (chains-may-replicate was always the fallback; a dead ghost must not veto keygen forever). Same round: desktop vault flagged STORAGE DEGRADED (dual-ring) at ~17:xx — Complete-state save still succeeded; inspect the degraded ring before trusting long-term (separate item, don't conflate).
- **Lane graveyard GC — VERIFIED + CLOSED 2026-08-30**: shipped 481eb9f; the probe came back better than predicted — blob 920ee297 reads 67,492 bytes (was 8.1MB, target was ~170KB) and ZERO lane-prune lines in the whole evening's logs, so the graveyard is gone AND not regrowing (no rotation-rate cap needed on this evidence). Its one remaining ~900ms SLOW put is the flat per-commit flush cost — the group-commit ticket's territory, not size.
- **Deploy blocker, leviathan — NAMED + CLEARED 2026-08-30**: never a build failure — the v1 fleet-op verify SUNSET tripwire (fgtw/fleet.rs const assert, release-profile only via the version bump crossing 0.70.0), which is why it only died in deploys and cargo check stayed green. The conscious bump was taken (0,70,0 → 0,82,0) with the evidence in the const's comment: v2 re-founding is impossible while succession EMIT is unwired, and Daniel is pre-flag-day — deleting v1 verify would break folding every real chain. At the 0.82 firing: wire succession emit, run the re-pin fleet-wide, then DELETE the path (flip V1_FLEET_VERIFY_PRESENT), don't bump again.

- **DATA LOSS: relaunch erased messages on the Linux box — CONVICTED + FIXED 2026-08-30 (6a71446), verify then close**: the suspect list had it backwards — `save_messages` is upsert-only and can't delete a composite row; the killer was its legacy-Int migration sweep, which assumed the snapshot held the whole table: a conversation touched before its rows loaded (conv_mut_of materialized EMPTY, and a receive could beat the boot loader to it) persisted its few RAM rows and the sweep then deleted every legacy row it never re-put. Four layered guards shipped: materialize now loads the durable table on first touch; persist REFUSES an un-hydrated conversation (loud, signal rows fail); the refusal self-heals via late hydration (disk rows merged under RAM rows thru the (timestamp, content) collapse); the sweep is timestamp-matched — an Int key with no re-put twin is not ours to touch. Plus the honest shrink tripwire: a snapshot smaller than the durable table logs both counts before writing, which is the capture for the two unproven suspects (read-cache-empty, phantom conv-id). WATCH: any "persist REFUSED / late hydration merged / snapshot has N but table holds M" lines in submitted logs.

- **Friend-history key divergence — NOT REPRODUCING 2026-08-30, watch-then-close**: the Emma-online round the instrumentation waited for happened (six logs: desktop, phone, 2× MacBook, both Emma devices) and the skf diagnosis line NEVER fired — zero `page open failed … ours key# theirs key#`, pages merge (`0 new of 49 rows`), the expiry storm is gone (hundreds/burst on 08-29 → zero in the fresh pull), and no `BLIND: CRITICAL divergent S epoch` either (the BLIND noise was `unknown token or unauthorized device` — the cacbc223-fold lesion, healed by the MacBook update). The 2026-08-21 wedge predates skf so it can't be retro-attributed, and it may never have been keys: the large-frame PT ticket below (frames of exactly 17,328 failing every parser) produces identical symptoms with matching keys. The skf line stays as the permanent tripwire; the design follow-ups (history adopt-iff-newer era reconciliation, S-epoch higher-wins) queue behind an actual firing, not a ghost. WATCH: any `ours key#X theirs key#Y` in a submitted log reopens this with full attribution.

- **PT multi-packet transfers livelock on one-directional UDP — PT ACKs have no relay leg (CONVICTED 2026-08-31, live capture)**: the android-deploy bridge final (106,268 bytes / 104 packets, hash 50f54f6a) AND a 4,540-byte / 5-packet sibling transfer both looped for minutes on the Mac guest — SPEC accepted → DATA arrives → our PT ACKs (raw UDP to peer_addr, no fallback) die on the Mac→leviathan direction (punch validated leviathan→Mac only; the reverse is NAT-blocked) → sender retries 5×/packet, gives up ("advancing the queue"), message layer re-offers the WHOLE transfer → re-SPEC forever. Reassembly is EXONERATED (2026-08-30 boundary capture: the rejected 8,695-byte hist_page was VALID signed VSF — the "no parser claims" drops are a separate dispatch/refusal question, likely the cacbc223-era sealer). Chat survives the same path because single-datagram frames ride the message layer's relay fallback. NEXT: give PT ACKs (and NAKs) the same relay leg chat has — route them via send_via_relay when the transfer's source is a peer with no validated OUTBOUND path; the sender's existing dedup makes doubled ACKs free. Watch also: "Inbound transfer timed out" against stale-LAN sources (192.168.1.163) is the same stale-address ladder noise, not a second bug.

- **HEAD-GAP coverage hole wedges the lane permanently — the ROOT under the dead calls** (CONVICTED 2026-08-31, both devices on a0691d7; supersedes the earlier narrower ANSWER-gap + "calls dead both ways" entries): Nick called Emma twice (cellular + LAN); NEITHER rang, both failed IDENTICALLY because the wedge is at the CHAIN layer above transport — path irrelevant, which is exactly why switching to LAN didn't help. Emma's phone (8b29d719) is stuck 2 rows behind (she has 263, Nick 265), pinned at head 33c89ac2; every Nick frame — the call offer included — arrives with an unseen `prev` and the chain CORRECTLY refuses to decrypt it (72 `Hash chain gap … buffering (ahead of us)` lines). No offer decrypt → on_call_signal never fires → no ring (Emma's log has ZERO CALL lines). THE BUG (matches the design intuition "can't decrypt latest without the history, so it must reconstruct"): the 2 missing rows are a HEAD gap (NEWER than Emma's tip), but reconstruction only covers the TAIL — history recovery walks BACKWARD from oldest (`requesting page before HEAD` → `0 new of 50 rows … complete=1`, a lone `3 new` once, never closes it). A head gap is supposed to heal via re-serve, but Nick's re-serve (a) fires at device 8d2d1b2b, not the wedged phone 8b29d719, and (b) caps + PARKS: `re-serve cap spent … theirs 263 vs ours 265 — parked until their sync record moves` — her count can't move because she can't apply the rows, so Nick parks: standoff. So the head rows fall in a coverage hole — too new for tail-backfill, marked delivered so live-resend won't fire, re-serve capped/parked/mis-targeted. NEXT (root, ownerLESS per FLEET-HOLDS-HISTORY — Nick 2026-08-31): the heal must NOT designate one owner or one target, because any device on either side may be off (clamshelled MacBook, powered-down Android) — the log's `re-served … to 8d2d1b2b` served ONE Emma device (likely her clamshelled laptop) while the wedged CALLING phone 8b29d719 never got the rows, and only one Nick device re-served. Every LIVE device of the sender's fleet independently serves a behind/gap-buffering peer from its own byte-identical durable store, redundantly (idempotent dedup makes duplicate serves free), and serves the SPECIFIC device that is gap-buffering (advertised head < ours and frozen) — no cap-park on that device, bypass for the exact missing tip..tip+deficit HEAD rows. Symmetric: whichever peer-fleet device receives heals its own siblings via normal fleet chain sync. So a heal completes as long as ONE device on each side is up, which is the resilience the fleet is supposed to buy; a single-owner/single-target serve is the anti-pattern. Equivalent framing on the digest path: a sender that has MORE than the peer force-PUSHES the head delta from every live device, instead of only gating a pull. Layered fix (call-specific, still wanted): call signals apply on ARRIVAL by call_id match (idempotent, crypto verified at decrypt) while the row still stores for ordered history — a ring then survives a transient gap even before the head heal lands. Also same round: Emma's persistent `no validated direct path` (cell IPv6 privacy-address churn) — punch-on-dial-edge + relay media remain deeper items (docs/calls.md). FRESH EVIDENCE (2026-09-01, Nick-phone cacbc223 + Brittany c9b5f417 logs, second couple, wedge PARTIALLY healed — her ring landed 1s after his dial, so offers now traverse): what remains is (a) `re-serving 8 row(s)` → `re-served 1 row(s)` on every burst — the SERIAL-SEND gate holds re-serves behind the in-flight row, so a 2-burst cap spends having actually served ~2 rows, and a hole deeper than that parks forever; the re-serve must either bypass the serial gate (rows are already-chained at original stamps) or the cap must count rows SENT, not bursts attempted; (b) `re-serve cap spent at tip 0 (theirs 260 vs ours 266)` — a tip-ZERO testimony with a 6-row deficit parked on the counting-divergence assumption; tip 0 means the peer never applied ANY row of that lane (era-split debris), which the park treats as convergence; (c) the busy-path explained the 'linux box rang' report: the phone was mid-outgoing-call so her call-back correctly hit Busy there and rang the idle desktop — multi-device ring is by design. Also chronic in the same log: `FGTW: a Pending contact has no address` every 15s for hours (a Pending-forever contact, era-split debris class, separate ticket-worthy noise).

- **Friend-side ghost refusal never trips**: Emma's phone pings Nick's LOCKED dead desktop 90e571bf and gets answered by cacbc223 instead (`pong answered by cacbc223 but we pinged 90e571bf`, 21× in one log) — the reported-stolen pong-tail signal needs TWO distinct reporters and evidently hasn't crossed the threshold at Emma despite Nick's fleet having 3+ live devices. Verify Nick's devices actually EMIT the locked set in their sealed tails (set_locked_report wiring) and that Emma's intake counts distinct reporters correctly; until it trips, every friend of a fleet with a locked device burns ping cycles on the corpse forever.

- **manifestus tramped blocks — CLASS CONFIRMED DEAD 2026-08-30, closed-with-watch**: the 84 (08-25) and 173 (08-27) danglings traced to `FileDev::grow` refusing at-or-below targets after a crash between fallocate and the spine commit; fixed manifestus 7d42301 (08-27). Confirmation: the ONLY repair event in six device-logs of evidence — the phone's 265-dangling repair, 08-29 03:48 — ran v0.67 (built 08-25, PRE-fix), so it's the disease caught once more on an old build, not counter-evidence; every post-fix boot is clean (phone v0.69 sessions: zero repair lines; desktop dev build: clean opens thru a half-dozen same-night kill-and-relaunch cycles — exactly the churn that used to mint danglings). Caveat that keeps the watch: the phone's post-fix boots followed its same-day repair (fresh vault), so one ordinary phone restart on an AGED vault opening clean is the final nail. WATCH: any `repair open … dangling` line on a v0.69+ build reopens this.

- **kete group-commit re-land — MOTIVATION RECONFIRMED 2026-08-30, HIGH again**: "most of the original motivation is gone" is contradicted by fresh evidence — the desktop logged 257 SLOW put/delete lines in one evening window, worst 3.3-3.7 SECONDS for values of a few hundred bytes, each holding the vault mutex ("every vault caller blocked behind it"); phone and MacBook show zero, so this is the desktop's per-commit flush cost paid once per op, exactly the flat-latency class the reverted batch drain (1944ebe) amortized. The revert (d48f294) was correct — batching N puts under ONE kete commit starved manifestus's rollback fence (fence slides on commit only; the burst exhausted it, every persist failed, durable-then-signal withheld ACKs, clutch stranded at 11/12) — and its comment names the real design: a BATCH COMMIT INSIDE MANIFESTUS that slides the fence exactly as N sequential commits would, proven by an equivalence test (batch of N → same fence position, same restorable generations, same crash-recovery surface as N commits) before kete's drain returns. Storage-engine work, fresh-session sized; the 63edff4 slow-op probe keeps scoring it meanwhile.

## WAITING ON A FIELD ROUND (nothing to build first)

- **Bridge Phase 1 + 2 field verify — CORE VERIFIED 2026-08-30**: multi-command sessions work Mac guest → leviathan (the stop-hang killed at 889a13a: re-served finals arrived wireless, exit never stamped; masked in the field by a host that silently never rebuilt). Residue to exercise: three-press escape, Stop against a genuinely long command, locus strip across a `cd`, and the known cosmetic gap — a re-served frame doesn't refresh host/cwd.
- **Consent gate live test**: add one-way, verify the other end sees *nothing*; add back, verify the knock completes the ceremony. Friend-fleet only — siblings don't exercise it.
- **Blind ops (private identity S) E2E**: probe → generate → deposit → ack → recover has never run end to end across real devices since the party-id token seam was fixed (2026-07-16).
- **Fleet message sync E2E**: send on A / watch B, fresh-join backfill, delivered-tick propagation (shipped @c6d6285, never live-tested).
- **Daniel's device**: still pre-flag-day — needs an update plus an online overlap to complete the re-key.
- **Ghost decommission**: 90e571bf (pre-wipe home desktop) is locked out fleet-side; the hardware still needs a physical wipe when reachable.

## NEXT — actionable, queued

- **Single-device-owner audit — four more duties owned by a device that might be off** (swept 2026-08-31, same disease as the HEAD-GAP ticket's single-target serve; doctrine: FLEET-HOLDS-HISTORY means NO duty may be pinned to one device, because any device may be clamshelled/powered down — every live sibling executes redundantly, idempotence makes duplicates free). Ranked: (1) **`active_device` ping pinning** (contact.rs `active_device`, status.rs bootstrap un-deadlock ~812): a friend contact has ONE ping/retransmit target; if that device sleeps, pings chase a corpse until the offline edge rotates — the friend's live devices sit idle (the code's own comment admits it). Fix: fan pings to ALL known device endpoints, dedup at the receiver; likely a contributor to grey-flash ring flakiness. (2) **Fleet-forward transmit** (unification v1): compose-anywhere works but the CHAIN OWNER transmits the forwarded rows — owner clamshelled ⇒ the message sits until it wakes. Any fleet device holding the replicated chain can transmit on its own lane (per-device lanes exist for exactly this). (3) **Avatar pin re-publish after removal-heal** (settings.rs ~339-390): the rotation-race winner publishes the new avatar pin one-shot; crash before publish ⇒ avatar stuck under the revoked key. Ownerless repair: any device that notices pin-epoch < fleet-key-epoch re-publishes (idempotent). (4) **Doorbell sibling bell overwrite** (doorbell.rs, open since v1): siblings overwrite each other's bell registration, so "the bell" is whichever device wrote last, not the fleet — per-device bell slots (or a merged set) so every registered device can be rung. NOT violations, leave alone: per-device lanes (single-writer by signature), WFD group owner (radio protocol), FCM tokens (inherently per-device), self-signed departure, CLUTCH ceremony_owner (single-owner-AT-A-TIME with TTL succession — deliberate §4.2 competing-instance guard).

- **Android inset-native rendering — the edge-to-edge opt-out has a shelf life** (2026-08-30): targetSdk 35 force-enables edge-to-edge (surface draws under the status/nav bars — trampled them on the first SDK-35 build); the shipped fix is the `windowOptOutEdgeToEdgeEnforcement` theme overlay (values-v35), which Google deprecated ON ARRIVAL and has said stops being honored at a future targetSdk (signals point at 36 / Android 16) — roughly a year of runway, more since we sideload. The real fix is small for photon because fluor owns every pixel and the IME inset mirror already exists: extend the SAME JNI path (`nativeImeInset` pattern) to mirror the status-bar + nav-bar inset rects, and have the layout pad its top band and bottom strips by them — then edge-to-edge becomes a feature (our pixels behind the translucent bars) instead of an imposition. Do it deliberately, before a targetSdk 36 bump ever forces it in a hurry.

- **Off-grid: Wi-Fi Direct v1 BUILT 2026-08-30 (docs/offgrid.md), field verification + later rungs pending**: tier ladder decided LAN → WAN → Wi-Fi Direct → relay-last (all ciphertext, so tiering is pure cost); BLE demoted to pairing-only; never-disconnect-infra is a hard constraint (STA+P2P concurrency). Built: `wfd_cred` per-pair pre-provisioning over the friend channel (lower-pubkey device = GO + minter, epoch-adopted, persisted), rotating DNS-SD friend tokens, edge-driven bearer (stranded ⇔ relay-down ∧ friend-unreachable), no new transport (group members ride the main UDP socket via unicast pt_disc + `HostV4P2p` candidates), Android PhotonWifiDirect.kt + JNI bridge. Also built same day: OPEN HOUSE new-friend-in-the-woods add (docs/offgrid.md §open-house) — offline add submit arms an ephemeral group with cleartext creds in the TXT beacon, the peer's pt_disc beacon is the registry record, dual-open-house SSID tie-break. PENDING: two-phone field test (same-AP no-engage, woods group-up, open-house add, concurrency, teardown), Linux wpa_supplicant D-Bus rung (`wfd-linux`), Windows WinRT / mac MPC stubs, LoRa horizon.

- **Waihanga goes live — WHEN multi-agent work starts, not before** (verdict 2026-08-30): the crate (/mnt/Harbor/Code/waihanga) has real ticket types + a real VSF schema (one `ticket` section per file, `ticket_index` sibling, author = hP handle proof) but EVERY I/O fn is `todo!("ticket 0005")` and the schema targets crates.io vsf 0.3 while the living vsf is 0.9.2 — so it cannot hold a ticket today. TICKETS.md outperforms it for the current single-repo one-agent flow (six tickets triaged in one evening via grep+edit+commit) and stays. Making it live is one bounded session: repoint `vsf = { path = "../vsf" }`, port the schema to 0.9.2, implement 0005 (write/read/index + settle the comment representation it left open), then migrate this file mechanically. FIRST: the local copy is NOT a git repo (Cargo.toml names github.com/nickspiker/waihanga but there's no .git) — init/clone before touching anything.

- **Fleet epoch spine, residue after the stale-entry audit (2026-08-31)**: legs 2+3 + the traffic mint SHIPPED 1e327e0 (2026-08-16) and this entry didn't hear — sibling pong tails ride `sibling_pong_seal_key_epoch` (per-checkpoint rotation), `hist_page` seals under `fleet_epoch_seal_key` with k/k−1 accept, the sweep mints at 128 fresh rows. STILL TRUE: refold-time reconciliation of adopted epochs against the chain's commitments; custody rewritten only by checkpoint winners (a rotation with no subsequent checkpoint leaves the slot under the old fleet key — ckpt_state serves the gap); the identity-seed pong derivation survives only as the pre-spine bootstrap fallback, by design.

- **Stuck lane, the nonzero-tip case**: rotation handles the anchor wedge; a mid-chain regression is undecidable from osc heads alone. Needs per-lane head HASHES in the sync records — "the peer's expected-prev is a hash I can serve" beats "the tip number moved".

- **§4.2 CLUTCH ownership — stage A, transferable lease**: ownership becomes a lease any awake sibling can claim (epoch + device_pubkey monotonic tiebreak, rides the fstate hub push, ~30s silence → re-claim). No wire change; the friend side already accepts any folded member's offers. Kills the dozing-owner stall, which has now bitten twice (peer_m's re-key waiting on a sleeping phone; Emma's re-key parked on the phone 2026-08-26). Stage B (race-and-converge, min(ceremony_id) wins) waits for the primary registry and makes fork-healing a normal mechanism — decide that deliberately.
  - Ships WITH: wire `consent_mutual` into the roster LWW ride. Today a sibling stub materializes Mutual, which is harmless only because ceremony parking stops non-owners racing — a lease TAKEOVER of a never-mutual pending add could fire an offer from a device that never saw the WeAsked state.

- **CLUTCH sibling-race**: a peer's sibling initiating CLUTCH before we fold it into the contact's fleet is rejected by the PT gate and gives up before the fold lands (~20 min stall observed). Fix = re-initiate after the fold, or queue the offer until the fold verdict.

- **Per-device About on the Fleet page — BUILT 2026-08-30 (fd3f825), field-verify then close**: the sealed pong tail carries a typed `abt` field (version · commit · os arch; optional, unknown-field-skip, no flag day) and every fleet row renders it under the device name — self shows its own build, siblings fill in as their tailed pongs land. A stale version on a row IS the not-updated indicator. VERIFY: open the Fleet page once two devices run ≥ this build and read each other's rows; OS *version* (beyond os+arch) is a possible v2 if wanted.
- **Bridge host for every platform**: Windows (cmd/PowerShell shell model, pgrep-free interrupt tree), Android (`/system/bin/sh` + a `/proc`-walk child tree; app-uid sandbox, but direct log/vault access on the least-reachable device is the diagnostics win), redox if its process API allows. Non-host builds already refuse with a typed "(no shell on this device)" final.
- **Bridge leftovers**: real-PTY opt-in mode for interactive programs (vim/top/REPLs); delete the legacy untyped-command arm once the whole fleet is past v66; host-side stream HANDOFF across a host restart (re-attach to the detached command's output) if the deploy-through-bridge case stays annoying.

- **Compose-anywhere adopt-edge unlock**: a sibling that adopts a woven blob can mint its own lane and send — seal `chain_woven` on the chain-sync adopt edge, retire "secured on <device> — send from there", delete the fleet-forward fallback (lanes.md marks it dead code once proven).

- **Message-format rework, remaining string markers**: the DELETE marker (`prefix + decimal ts`, needs a dual-parse bridge + tombstone reconciliation — the natural next `RefKind::Delete`), the chain-probe sentinel, the attachment record-in-content (hex hash + decimal size), `profile._custom` (`id\tlabel` lines). Their rows are deployed, so each needs its own migration pass.

- **TWIN-DEVICE detection** (field 2026-08-21): two photon installs on one Android share ONE device identity and BOTH attest legally — ANDROID_ID is per-signing-cert, and D2's gate checks the (device_key, identity) PAIR, which the twin satisfies exactly. Protocol state splits across two vaults; server-side the twins are cryptographically indistinguishable, so diagnosis requires the phone. CHEAP FIX: at launch, enumerate packages declaring `com.photon.SESSION_READ` (the signature permission only TOKEN apps hold) and refuse to run past a loud screen if a second one answers. The deeper fix (fingerprint mixes the package) would end the class but is a flag day for every Android device identity.

- **Per-field profile disclosure + wipe-restores-everything**: build together, because the cloud blob is all a wiped device gets back — whatever it omits is GONE (proven twice: contacts returned with avatars and no names; an avatar orphaned by a re-minted pin). Model: You-page fields are VSF `x` text, each with a "share by default" checkbox (default off); display name is the one mandatory always-shared field; per-contact toggles live on the contact panel (checkbox + non-editable current value); the recipient side is a struct where every field is optional. The restore blob must carry the full received-disclosure struct per contact WITH per-field LWW stamps, plus our own profile. Acceptance test for every field added anywhere: does a wipe restore it? Crypto design in docs/contact-system.md.

- **Render-pass sub-profiler**: the status drain has one (`PERF: status pass Nms … top arms:`); the render pass doesn't, so 5.8/8.3/8.8s single Conversation renders (field 2026-08-21) name no stage. Mirror it — per-stage cumulative ms on any >1s pass (bg, rows, text shaping, avatars, damage).

- **Hit-id bookkeeping**: short term, one source of truth for the settings pill bands (reservation / dispatch window / stamp sites are three hand-synced numbers — the Lock-out pill bug). Long term, press-captures-meaning dispatch: ids per frame, release fires only if the widget under the cursor resolves to the SAME action. Content-hashing is OUT — the id space is 16-bit and birthday collisions would hit every user.

## TRANSPORT — direct-path hardening (ordered; land + 2-device test each)

- **Sibling gossip as the middle address tier**: ask a reachable sibling for a peer's `PeerRecord` before falling back to the seed registry. The registry tier landed 2026-07-31 (`pb_put`/`pb_get`, self-signed records, verified live); this closes the remaining single-point dependency on a device's OWN fgtw reachability — the failure that stranded a whole ceremony when 4/7 fgtw fetches failed on cellular. Also retire the 15s full-resume pulse (it re-runs the whole attest; should be announce-only or doorbell-driven).
- **TCP as a direct path (T1.3)**: `tcp.rs` has no inbound accept — TCP is only a whole-payload PT fallback to a known UDP address. Add an inbound listener on `PHOTON_PORT` feeding the same receiver pipeline, plus a TCP connect raced alongside the UDP punch. Wins UDP-blocked networks and makes a forwarded host just work. Test: UDP-blocked client reaches a forwarded host.
- **IPv6 as first-class (T3)**: `gather.rs` prioritises HostV6 but full local-interface enumeration is deferred and the announce may carry no global v6. Biggest single win for a cellular peer on a v6 carrier — bypasses CGNAT entirely. Test: two v6 hosts connect with zero punch.
- **Server-coordinated simultaneous-open (T2)**: targeted WS delivery in the worker (subscribe by device_pubkey) + a punch-request message, so a cold pair connects on the first attempt. NEEDS A WORKER DEPLOY.
- **NAT relay tier**: symmetric↔symmetric (two cellular peers) has no direct path at all. Friend-fleet devices as relays fits the peers-are-fgtw direction.
- **Mac wake for messages/calls** (LOW, approved 2026-08-21): stage 1 Wake-on-LAN sibling knock (capture MACs during LAN discovery, magic packet → ~3-5s → TCP knock); stage 2 Bonjour sleep-proxy registration of `_photon._tcp` (covers friend traffic, needs an Apple proxy device present). LAN-only either way; hibernated Macs wake for nothing.

- **One-directional-path doctrine (2026-07-31)** — keep applying it: three subsystems died the same death in one day (blind srv answers, message ACKs, history page serves) — a validated path whose REVERSE direction silently drops. Rule: small idempotent response frames ALWAYS carry their relay copy alongside any direct leg. Validation proves OUR probes round-trip, never that the reverse delivers. Suspect the same mechanism in punch-probe replies.

- **Relay policy convergence — escalation, not promiscuity** (Nick approved 2026-08-06, land after the field round): the relay is a metadata oracle, so parallel copies of big payloads double exactly the traffic whose cost is material. End state: unconfirmable-and-small (ACKs, ClutchComplete, weave probe — no failure edge exists) go parallel always; confirmable (chat, anything ACKed) go direct-first on a trusted path with the FIRST retransmit carrying the relay copy; large (offers, avatars, history pages) never parallel — fast-fail direct, then relay-only.

## DESIGN — decided, unbuilt (home docs carry the detail)

- **Fleet-assisted attest** ("I hold the vault, my friend holds the key", designed 2026-08-07): auto-attest after reboot with NO durable identity secret at rest. The capsule is sealed under a random `wake_key` the device does NOT keep; each sibling holds a share in its own vault under the fleet key. Boot → derive device keypair from the machine fact → signed WAKE_REQUEST over relay + LAN → sibling grants iff signature valid AND in current fold AND not locked → open, attest, ROTATE immediately (a captured share is single-use). Critical constraint: NEVER seal the capsule under a machine fact — `identity_seed`'s handle derivation is deliberately cheap, so a stolen durable capsule falls to a dictionary sweep and then passes the lockout's handle gate. One-device fleets can't enable it (no share can exist). Policy knob: auto-grant (unattended) vs require-approval. Related: docs/device-trust-and-recovery.md.
- **Identity profile stage C** (docs/identity-profile.md): worker `profile_put`/`profile_get`, identity-signed envelope verified against chain genesis, client blob v1 with grants sealed to each friend's pinned identity pubkey; publish on name-set + friend-add, adopt into `published_name`. Until it lands, contacts render as keyed voca pseudonyms. Beyond C: per-field keys + share checkboxes (merges with per-field disclosure above), profile key epochs, grant-carried friendship salt, NFC invite card. Petname editor is DEAD (PRST5).
- **Pairing v2 leftovers** (docs/pairing-v2.md): NFC tap transport for device-add (HCE, candidate machine built, tap delivers); BLE transport (lock word + proof beacon, shadow beacon proven); Security-page "Remove this device from fleet" is a stub (`depart_device` exists, unwired).
- **Identity / device lifecycle punch list** (docs/lifecycle.md — the canonical flow tree): (1) redeploy the fgtw.org worker so the live one-owner-per-device gate matches source, verify with a scratch two-handle genesis; (2) JoinerSelected green flood + sponsor-confirm hold; (3) collision counter + notifications toggle on Panel→Fleet. LastRites (D3) is SUPERSEDED — identity never dies (no terminal op exists; the worker refuses zero-member folds).
- **Profile rework (D)**: ONE key per base (`profile.addr`), instances as multi-value rows, identity as TAG (home/work/custom) — kills `profile._custom` and the decimal-suffixed `addrN` keys. Held.
- **Lockout follow-ups**: UNLOCK SHIPPED (handle-confirmed, fires inside the next successful attest — `pending_unlock`); still open: Fleet-page scroll (rows `take(6)`, devices past six invisible); the verifiable single-report upgrade (a report carrying proof of current-epoch possession lets friends verify instead of trust).
- **Device loan** (Nick 2026-08-28, from the borrow-a-phone case): lending is LOCK (session cleared, vault intact, re-unlock by handle) — that path exists and is right. The unsolved half is the borrower needing photon AS THEMSELVES on the loaned device: the one-identity-per-device gate refuses, and today shred is the only way past it — which destroys the lender's local state for a temporary guest. Needs the loan-annotation vs transfer decision (memory: project_device_loaners); whatever ships, the DEVICE BUSY screen must offer the guest path instead of implying shred.
- **Lanes leftovers** (docs/lanes.md): per-lane label rotation at re-key epochs; fork-on-a-single-writer-lane becomes a loud error (the gap-streak repair stands in today).
- **ferros exec/naming** (2026-08-27, notes in memory): no ambient cwd in the exec API, bind-don't-search (petname → blake3 + signature at spawn), VSF typed headers instead of `#!`, package roots handed to programs instead of `$0` reconstruction.
- **FULL COPY PASS — every user-facing word** (Nick 2026-08-28: "we'll need to go thru all the words. clutch ceremony, warnings, errors, all of it"): a deliberate sitting, not a drive-by. Audit every string a human reads — the CLUTCH ceremony ladder narration, every warning/error/toast/banner, the Security-page pill descriptions, the attest/interstitial screens — for (a) advice that's wrong for the reader's actual situation (the seed case: DEVICE BUSY says "Remove & shred" to someone merely LENDING a phone — right advice for giving a device away, destructive for a loan; must name both paths: lend → Lock, give away → shred), (b) jargon leaking (fold, weave, lane, re-key), (c) states with no words at all (the hour-long deploy with no completed/failed/running verdict). Sweep tool: grep the string literals out of render/launch/driver/devices and read them as a user.

## WATCH — verify from the next log round, no work until then

- Repair-open should find ZERO danglings on a second consecutive boot (closes the tramped-blocks item above).
- Phonebook row count should stay at the real device count (~6) — a climb means the dedup regressed.
- `SHADOW SEAM` lines identify remaining stale-era chains blobs worth re-keying.
- `PIPE: silent past the liveness window` proves the 45s ping / 120s reconnect catches silent pipe death.
- Status-pass profile lines (`top arms:`) name whichever arms still deserve async conversion — the standing suspects are HistoryPage/Request and ChainSync.
- FSTATE ping-pong should be quiet now that the era wedge heals; if it ever matters again, suppress the weave-seal bump when `woven` re-asserts an unchanged value.
- Re-serve "cap spent … parked until their sync record moves" lines carry the numbers that explain WHY two sides count rows differently (deleted/edit/reaction divergence).
- Deliberately still on the UI thread: CLUTCH KEM decap (~120ms, once per ceremony, entangled with the offer/KEM/complete state machine), the two once-per-ceremony chains saves, and startup vault loads. Move the decap when the ceremony code is next open — not as a drive-by.

## SHELF — small, low priority

- **Background doesn't scroll with Conversation or Contacts** (Nick 2026-08-23): Settings is the reference — each pane's scroll drives its background half. Feed conv `scroll_offset` / `contacts_scroll` into the bg translation the same way (render.rs ~:237 even promises it). Moving background with foreground makes it so scrolling is simply a memcopy rather than a complete redraw, thus requiring only the top smidge to get drawn when scrolling down and the bottom smidge to get drawn when scrolling up rather than redrawing the entire surface before the window edge mask and clip.
- **Contacts search-box focus glow damage**: stale glow lingers/clips on deselect — the bg pass's dirty gating skips the glow bbox.
- **Copy doesn't always reach the clipboard** (field 2026-08-21, intermittent): the write silently no-ops. When it recurs, log the clipboard set result + backend and confirm the copy path holds selection ownership past the event.
- **Dock/taskbar click sends photon BEHIND the other app and the click falls through** (field 2026-08-22, Linux): the resident-surface path doesn't claim focus/stacking atomically. Needs explicit raise + request-attention + focus in the right WM order. Cousin of the macOS multi-monitor vanish.
- **macOS notification click launches Script Editor**: `osascript display notification` attributes the banner to Script Editor. Fix = `UNUserNotificationCenter` from the .app bundle. Interactive-only to verify — land it during a live mac session, never blind.
- **Attest wrong-handle UX**: map the worker's one-owner rejection to "This device is already bound to another identity — type its handle to resume, or wipe first"; and after that error the blinkie + textbox are dead until click-off/click-on (the Error path's refocus leaves focus state desynced).
- **Textbox glow on search state**: legacy tinted the search pill yellow-during / green-or-red-after; fluor's glow is focus-driven, so this needs a small fluor affordance (`set_glow_colour`) plus wiring.
- **Party colours are placeholder**: swap to perceptual L≈50% via vsf spectral/LMS.
- **Post-attest multi-device prompt**: redundancy IS the recovery story — prompt for a 2nd device right after attest, reflected in a Security/Recovery posture strip.
- **Updates**: wire the Android AUTOMATIC path to auto-download+install (gated on unmetered/wifi detection first — the label says "Check" not "Install" there until then); `.prev` rollback with auto-revert after N failed starts; idle gating + in-flight textbox hand-off across the re-exec (a mid-typing swap loses the compose box); heal the live dev manifest (Android row claims 0.36.12 but carries the 0.36.11 APK — re-run the publish scripts, which stamp correctly now).
- **Log trim audit**: the seconds-vs-oscillations question in `trim_log_file` is UNPROVEN speculation — the 2026-08-01 "confirmed bug" was retracted (a wipe deletes the log, so the 16-second file was 16 seconds OLD). Take an honest measurement first: a device with >24h of uninterrupted history submits, and the span is checked against the 16 MiB window.
- **Blinding flag-day debris**: `photonlog` still derives a submitter seed from a typed handle (dev tooling — wants a party-id flag); `search_status`/`SearchResult` carry the typed handle transiently for the "added {h}" toast (in-RAM only).
- **fluor**: Android multi-touch (pinch-zoom waits on a multi-touch `Touch` event); drag-to-move torus wrap regression (2026-08-20 — `shift_screen_wrap` still rotates, so suspect what's IN the buffer at drag time); macOS resize arrows missing at window edges; Wayland drag-and-drop (blocked on winit #1881 / PR #4504); **macOS multi-monitor window vanishes on cross-monitor drag** (PINNED, needs a mac debug session).
- **Platform**: TOKEN session relay (spec'd, photon's side wired — deferred until a second TOKEN app exists to test with); serve the APK so Chrome offers install; macOS softbuffer present-on-clean re-verify; `dev-adb.sh` sometimes reuses a stale-built `.so`; Samsung colourspace rejection (`setBuffersDataSpace` returns EINVAL, so the calibrated BT.2020 path never takes — needs a capability probe with a BT2020 → P3 → sRGB fallback chain and the LUT re-derived against whichever took); Android API 26-29 IME inset (all fleet devices are 30+, revisit only if an old device joins).
